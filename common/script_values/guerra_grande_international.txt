# EN: Provisional game pounds, not a claimed historical transfer. Provincial fragmentation caps aid.
# ES: Libras de juego provisorias, no una transferencia historica atribuida. La fragmentacion limita el auxilio.
gg_int_arg_aid_capacity = {
	value = 20000
	if = {
		limit = { global_var:gg_delegating_provinces < 10 }
		multiply = 0.5
	}
}

gg_int_arg_aid_cost = {
	value = global_var:gg_int_arg_quote
	multiply = -1
}

# EN: Mutually exclusive dossier states, based only on the existing transaction marks.
# ES: Estados excluyentes del expediente basados en las marcas existentes.
gg_ui_aid_state = {
 value = 0
 if = { limit = { gg_int_arg_offer_pending = yes } value = 1 }
 if = { limit = { has_global_variable = gg_int_arg_accepted } value = 2 }
 if = { limit = { has_global_variable = gg_int_arg_refused } value = 3 }
 if = { limit = { has_global_variable = gg_int_arg_withdrawn } value = 4 }
}

