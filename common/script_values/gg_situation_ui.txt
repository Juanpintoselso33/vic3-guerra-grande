# EN: Read-only presentation values; no new counters or military rules.
# ES: Valores de presentacion de solo lectura; sin contadores ni reglas militares nuevas.
# EN/ES: C10 -- the sea's UI values moved to gg_rio_plata.txt (gg_ui_plata_*)
# EN: Current diplomatic state only; higher-priority conditions override lower ones.
# ES: Solo el estado diplomatico actual; las condiciones prioritarias prevalecen.
gg_ui_war_diplomacy = {
 value = 0
 if = { limit = { has_global_variable = guerra_grande_open_war } value = 1 }
 if = { limit = { has_global_variable = gg_peace_cooldown } value = 2 }
 if = {
  limit = { OR = { has_global_variable = gg_peace_offered has_global_variable = gg_peace_chain_pending } }
  value = 3
 }
 if = { limit = { has_global_variable = gg_truce } value = 4 }
 if = { limit = { has_global_variable = gg_peace_settled } value = 5 }
}
