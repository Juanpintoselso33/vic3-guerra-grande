# EN: 24/9/2026, Juan (AUD-ERR-CUARTELES): a country born from Argentina keeps Argentina's army model.
#     la Argentina's law_local_caudillos is visible only to ARG (is_visible = c:ARG). create_country copies the
#     origin's laws, but a law the new country may not hold is dropped and the engine picks another army model:
#     the provinces were born with law_national_militia (+5 barracks per state instead of +10) and the engine cut
#     their barracks (Entre Rios 22 -> 5 on 1/5/1851). In Juan's save of 1853 ARG is the only country with
#     local_caudillos; the eight province countries born from it hold another model.
#     This is la Argentina's definition (common/laws/arg_army_model.txt, 2982907360) unchanged except for
#     is_visible, which adds gg_is_born_of_argentina (the thirteen province countries, the Coalition and Buenos
#     Aires, common/scripted_triggers/gg_provinces.txt). REPLACE like zz_gg_rosas_no_vote.txt; the mod loads
#     after la Argentina. gg_argentina_inherit_army_model then gives each newborn ARG's law of that moment.
#     If la Argentina changes this law, recopy it and keep the OR.
# ES: la ley de caudillos locales de la Argentina solo la ve ARG, asi que el clon la perdia y el motor le ponia la
#     milicia nacional y le recortaba los cuarteles. Es la definicion de la Argentina tal cual, con el is_visible
#     ampliado a los paises nacidos de la Argentina.

REPLACE:law_local_caudillos = {
	group = lawgroup_army_model
	icon = "gfx/interface/icons/law_icons/national_guard.dds"
	
	progressiveness = 0

	is_visible = {
		# EN/ES: Guerra Grande -- also every country born from Argentina (AUD-ERR-CUARTELES)
		#     / tambien todo pais nacido de la Argentina
		OR = {
			c:ARG ?= this
			gg_is_born_of_argentina = yes
		}
	}
	
	unlocking_technologies = { mandatory_service }
	
	modifier = {
		state_conscription_rate_add = 0.02
        interest_group_ig_landowners_pol_str_mult = 0.15
		state_building_barrack_max_level_add = 10
		state_building_conscription_center_max_level_add = 10
	}

    # AI will never return to peasant levies
	ai_will_do = {
		always = no
	}
	
	ai_enact_weight_modifier = { #Petitions
		value = 0
		
		if = {
			limit = { 
				has_journal_entry = je_government_petition
				has_variable = desired_law_var
                scope:law = var:desired_law_var
			}
			add = 750
		}
	}
}
