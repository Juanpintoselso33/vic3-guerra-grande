# EN: The Guerra Grande systems (docs/guerra-grande-flujo/to-be.md, C19, FG-1, FG-2).
#     T3a: variables and their single writers only -- nothing reads them yet. Every change to a
#     patron, to the Defensa's line or to the Cerrito's balance goes through one of these
#     effects, so the invariant check (I14) can prove nobody writes them anywhere else.
#     Starting values are provisional until the balance pass (T10).
# ES: Los sistemas de la Guerra Grande (docs/guerra-grande-flujo/to-be.md, C19, FG-1, FG-2).
#     T3a: solo las variables y quien las escribe -- todavia nadie las lee. Todo cambio a un
#     padrino, a la linea de la Defensa o a la balanza del Cerrito pasa por uno de estos
#     efectos, asi el chequeo de invariantes (I14) puede probar que nadie las escribe en otro
#     lado. Los valores iniciales son provisorios hasta la pasada de balance (T10).

# EN: Country scope. Clear the previous result so a landless country cannot reuse another HQ.
#     Keep the capital only while owned; otherwise use one of this country's states.
# ES: Scope de pais. Limpia el resultado anterior para no prestarle otro cuartel a un pais sin tierra.
#     Conserva la capital solo si es propia; si no, usa cualquiera de los estados del pais.
gg_select_formation_hq_state = {
	clear_saved_scope = gg_formation_hq_state
	if = {
		limit = {
			exists = capital
			capital = { owner = prev }
		}
		capital = { save_scope_as = gg_formation_hq_state }
	}
	else = {
		random_scope_state = {
			limit = { owner = prev }
			save_scope_as = gg_formation_hq_state
		}
	}
}

# EN: called once from guerra_grande_on_game_start.
# ES: se llama una sola vez desde guerra_grande_on_game_start.
gg_systems_init = {
	# EN: NO ELECTIONS BEFORE THE SPLIT EITHER. Vanilla starts Uruguay on law_wealth_voting, so the
	#     engine ran election campaigns from 1836, and vanilla's generic election events
	#     (events/election_events/, fired from on_monthly_pulse_country_elections, all gated on
	#     in_election_campaign) kept swapping the president for whichever interest-group leader they
	#     picked. The historical cast was being overwritten by a poll the country never held: under
	#     the 1830 constitution the President was chosen by the General Assembly, and between 1836
	#     and 1843 the presidency changed by civil war -- Oribe resigns in 1838, Rivera takes it by
	#     force -- not at the ballot box. law_oligarchy is the same shape the split already gives
	#     both governments from 1843, and it is what stops the campaigns: with no campaign, not one
	#     of those events passes its trigger.
	# ES: TAMPOCO HAY ELECCIONES ANTES DEL CORTE. Vanilla arranca al Uruguay con law_wealth_voting,
	#     asi que el motor corria campanias electorales desde 1836, y los eventos genericos de
	#     eleccion de vanilla (events/election_events/, que salen de
	#     on_monthly_pulse_country_elections y piden todos in_election_campaign) le cambiaban el
	#     presidente por el lider de turno de algun grupo de interes. El elenco historico lo
	#     pisaba una eleccion que el pais nunca tuvo: por la constitucion de 1830 al Presidente lo
	#     elegia la Asamblea General, y entre 1836 y 1843 la presidencia cambio por guerra civil
	#     -- Oribe renuncia en 1838, Rivera la toma por la fuerza --, no en las urnas. law_oligarchy
	#     es la misma forma que el corte ya les da a los dos gobiernos desde 1843, y es lo que corta
	#     las campanias: sin campania, ninguno de esos eventos pasa su trigger.
	c:URU ?= {
		if = {
			limit = {
				NOT = { has_law = law_type:law_oligarchy }
			}
			activate_law = law_type:law_oligarchy
		}
	}
	# EN: THE TWO SHARED STATES OF THE TANDEM (design 19/9/2026, after Codex mapped the Uruguayan
	#     chain). Today the Argentine side does not exist and the Uruguayan chain reads no Argentine
	#     variable at all: Codex's verdict was that "the code survives a diverted history only
	#     because it ignores it". These two are what will let it stop ignoring it.
	#     ORIBE'S CAMPAIGN IS THE HINGE. The army that decides the Argentine civil war is the same
	#     one that besieges Montevideo: out of Montevideo in October 1838, the Ejercito Unido de
	#     Vanguardia from Rosas, Quebracho Herrado, Famailla, Arroyo Grande, and across the river on
	#     16 February 1843. So the Uruguayan side needs to be able to ask two questions it cannot ask
	#     today: how the interior campaign ended, and what became of that army.
	#     BOTH START AT 0, WHICH MEANS "undecided -- behave exactly as history did". That is
	#     deliberate: while nothing writes them, every reader takes the historical branch and the
	#     Uruguayan side behaves precisely as it does now. A missing write cannot silently change a
	#     game.
	# ES: LOS DOS ESTADOS COMPARTIDOS DEL TANDEM. Hoy el lado argentino no existe y la cadena oriental
	#     no lee ni una variable argentina: el veredicto de Codex fue que "el codigo aguanta
	#     mecanicamente una historia desviada solo porque la ignora". Estas dos son las que le van a
	#     permitir dejar de ignorarla.
	#     LA CAMPANIA DE ORIBE ES LA BISAGRA. El ejercito que decide la guerra civil argentina es el
	#     mismo que sitia Montevideo: sale de Montevideo en octubre de 1838, Rosas le da el Ejercito
	#     Unido de Vanguardia, Quebracho Herrado, Famailla, Arroyo Grande, y el 16 de febrero de 1843
	#     cruza el rio. Asi que el lado oriental necesita poder preguntar dos cosas que hoy no puede:
	#     como termino la campania del interior, y que fue de ese ejercito.
	#     LAS DOS ARRANCAN EN 0, QUE SIGNIFICA "sin decidir: comportate como la historia". Es a
	#     proposito: mientras nadie las escriba, todo lector toma la rama historica y el lado oriental
	#     se comporta exactamente como hoy. Una escritura que falte no puede cambiar una partida en
	#     silencio.
	#
	#     gg_interior_outcome   0 sin decidir · 1 Rosas gano el interior · 2 lo gano a duras penas
	#                           3 Rosas perdio el interior
	#     gg_vanguardia_status  0 sin decidir · 1 cruzo entera · 2 cruzo mermada · 3 no cruzo nunca
	# EN/ES: gg_vanguardia_cruce  0 sin decidir · 1 cruzo todo · 2 reserva para el litoral · 3 cruce postergado
	set_global_variable = { name = gg_interior_outcome value = 0 }
	set_global_variable = { name = gg_vanguardia_status value = 0 }
	set_global_variable = { name = gg_vanguardia_cruce value = 0 }
	# EN: THE REVOCABLE DELEGATION -- the heart of the Argentine side. Rosas' power over the other
	#     provinces was never sovereignty: it was a DELEGATION of foreign relations that each
	#     legislature could take back, and taking it back is literally what Tucuman did on 7 April
	#     1840 ("withdrawing from him the delegation of foreign relations") and what Entre Rios did on
	#     1 May 1851 -- the Convention of 29 May says Entre Rios, "habiendo reasumido" the faculty
	#     granted to the governor of Buenos Aires.
	#     So the Pact counts HOW MANY PROVINCES STILL DELEGATE. It is not popularity: it is facts. If
	#     it reaches zero, Rosas has nothing left to be.
	#     THIRTEEN, AND WHY. The Confederation was fourteen provinces; thirteen of them delegated,
	#     because the fourteenth is Buenos Aires, which cannot delegate to itself. Every one of the
	#     thirteen exists as a state on this map.
	# ES: LA DELEGACION REVOCABLE, el corazon del lado argentino. El poder de Rosas sobre las demas
	#     provincias nunca fue soberania: era una DELEGACION de las relaciones exteriores que cada
	#     legislatura podia retirar, y retirarla es literalmente lo que hizo Tucuman el 7/4/1840 ("le
	#     retiraba la delegacion de las relaciones exteriores") y lo que hizo Entre Rios el 1/5/1851
	#     -- la Convencion del 29 de mayo dice que Entre Rios, "habiendo reasumido" la facultad
	#     concedida al gobernador de Buenos Aires.
	#     Asi que el Pacto cuenta CUANTAS PROVINCIAS SIGUEN DELEGANDO. No es popularidad: son hechos.
	#     Si llega a cero, a Rosas no le queda de que ser.
	#     TRECE, Y POR QUE. La Confederacion eran catorce provincias; delegaban trece, porque la
	#     catorceava es Buenos Aires, que no puede delegar en si misma. Las trece existen como estado
	#     en este mapa.
	set_global_variable = { name = gg_delegating_provinces value = 13 }
	set_global_variable = { name = gg_pact_war_urquiza_won value = 0 }
	set_global_variable = { name = gg_pact_war_rosas_won value = 0 }
	set_global_variable = { name = gg_pact_war_white_peace value = 0 }
	# EN/ES: cuantas provincias se alzaron alguna vez. No baja nunca: es la memoria de la campania,
	#     no su foto / how many provinces ever rose. It never goes down: it is the campaign's memory
	set_global_variable = { name = gg_provincias_alzadas value = 0 }
	# EN/ES: gg_oribe_siege_months removed 22/9/2026 with the Pact's redesign: nothing read it any more
	#     / quitado con el rediseño del Pacto: ya nadie lo leia
	# EN: THERE ARE NO PER-PROVINCE MARKS ANY MORE, and the reason is worth keeping. They were added
	#     this same day so the panel could ask each province with SelectLocalization; that did not
	#     render in Juan's game, so the list became sixteen plain descriptions chosen by script, and
	#     those read gg_delegation_gone_* -- which already existed. Thirteen marks nobody read were
	#     left over, and the inventory said so.
	# ES: YA NO HAY MARCAS POR PROVINCIA, y vale la pena dejar dicho por que. Se agregaron ese mismo
	#     dia para que el panel le preguntara a cada provincia con SelectLocalization; eso no renderizo
	#     en la partida de Juan, asi que la lista paso a ser dieciseis descripciones planas elegidas
	#     por script, y esas leen gg_delegation_gone_*, que ya existia. Quedaron trece marcas que no
	#     leia nadie, y el inventario lo dijo.
	# EN: THE THREE FEDERAL GOVERNORS WHO STAY WITH ROSAS, created on day one because that is when
	#     they were already governing. Vanilla has none of them, and without them the interior is a
	#     map with no one on it: Ibarra is the man who refuses to join the Coalition and cuts the
	#     northern bloc in two, Benavidez and Aldao are Cuyo, where Angaco and Rodeo del Medio are
	#     fought in 1841. They had templates and no event that created them -- which is another way
	#     of saying they did not exist.
	# ES: LOS TRES GOBERNADORES FEDERALES QUE SE QUEDAN CON ROSAS, creados el primer dia porque es
	#     cuando ya estaban gobernando. Vanilla no tiene a ninguno, y sin ellos el interior es un mapa
	#     sin nadie adentro: Ibarra es el que se niega a entrar en la Coalicion y parte el bloque del
	#     norte en dos, Benavidez y Aldao son Cuyo, donde en 1841 se pelean Angaco y Rodeo del Medio.
	#     Tenian template y ningun evento que los creara, que es otra manera de decir que no existian.
	#     Y LA GUARDA DE «LA ARGENTINA», que aca no es opcional: los tres tienen home_region en
	#     Santiago del Estero, San Juan y Mendoza, y en vanilla esos estados NO EXISTEN -- de toda la
	#     Argentina, vanilla tiene Buenos Aires, Corrientes, Jujuy, Santiago (que es la de Chile) y
	#     Tucuman. Sin «la Argentina» cargada, crearlos seria pedirle al motor tres hombres de
	#     ninguna parte, en toda partida oriental, aunque nadie toque el lado argentino. La sonda es
	#     la misma que usan los diez relevos del calendario argentino: Entre Rios no existe sin ella.
	#     / AND THE «LA ARGENTINA» GUARD, which here is not optional: their home regions do not exist
	#     in vanilla at all.
	if = {
		limit = { exists = s:STATE_ENTRE_RIOS }
		c:ARG ?= {
			if = {
				limit = { NOT = { any_scope_character = { has_template = ARG_gg_juan_felipe_ibarra } } }
				create_character = { template = ARG_gg_juan_felipe_ibarra }
			}
			if = {
				limit = { NOT = { any_scope_character = { has_template = ARG_gg_nazario_benavidez } } }
				create_character = { template = ARG_gg_nazario_benavidez }
			}
			if = {
				limit = { NOT = { any_scope_character = { has_template = ARG_gg_jose_felix_aldao } } }
				create_character = { template = ARG_gg_jose_felix_aldao }
			}
		}
	}
	# EN/ES: playtest -- countries kept in globals so journal texts can show their flags
	#     / paises guardados en globales para mostrar sus banderas en los textos
	# EN: THE ROLE MARK OF BUENOS AIRES. The journal's status line needs to know whether the reader is
	#     Rosas or one of the provinces that can take the delegation from him, and it asked that with
	#     c:ARG ?= this inside a status_desc trigger. The engine rejected it 1752 times in a minute of
	#     play on 20/9/2026: «Invalid right side during comparison 'this'» -- in a journal's status
	#     text, this is not a country, so the comparison has no right side. Vanilla's own journals only
	#     ever read variables there.
	#     So Buenos Aires gets a mark of its own, which is the same pattern the three new countries
	#     already use (gg_arg_corrientes_seat and its two brothers): the ROLE, not the tag.
	# ES: LA MARCA DE ROL DE BUENOS AIRES. La linea de estado del journal necesita saber si el que lee
	#     es Rosas o una de las provincias que le pueden retirar la delegacion, y lo preguntaba con
	#     c:ARG ?= this adentro de un trigger de status_desc. El motor lo rechazo 1752 veces en un
	#     minuto de partida el 20/9/2026: «Invalid right side during comparison 'this'» -- en el texto
	#     de estado de un journal, this no es un pais, asi que la comparacion no tiene lado derecho.
	#     Los journals de la propia vanilla ahi adentro solo leen variables.
	#     Asi que Buenos Aires lleva marca propia, que es el mismo patron que ya usan los tres paises
	#     nuevos (gg_arg_corrientes_seat y sus dos hermanos): el ROL, no el tag.
	c:ARG ?= { set_variable = gg_arg_rosas_seat }
	c:ARG ?= { set_global_variable = { name = gg_flag_arg value = this } }
	c:FRA ?= { set_global_variable = { name = gg_flag_fra value = this } }
	c:GBR ?= { set_global_variable = { name = gg_flag_gbr value = this } }
	c:BRZ ?= { set_global_variable = { name = gg_flag_brz value = this } }
	gg_plata_flags_sync = yes
	# EN/ES: patrons, 0-100 / padrinos, 0-100
	set_global_variable = { name = gg_patron_rosas value = 50 }
	set_global_variable = { name = gg_patron_francia value = 20 }
	set_global_variable = { name = gg_patron_britania value = 10 }
	set_global_variable = { name = gg_patron_brasil value = 20 }
	set_global_variable = { name = gg_patron_urquiza value = 40 }
	# EN: T5 -- phase 0 events score before Carpinteria creates the journal, so the score starts
	#     here; the journal's own immediate only sets it if it is still missing.
	# ES: T5 -- los eventos de la fase 0 puntuan antes de que Carpinteria cree el journal, asi que
	#     el puntaje arranca aca; el immediate del journal solo lo pone si todavia falta.
	set_global_variable = { name = guerra_grande_blanco_score value = 0 }
	# EN/ES: C10 (22/9/2026) -- the River Plate: signed months, consecutive shut months, total shut
	#     months, Rosas's stance (0 open) / el Plata: meses con signo, meses cerrados seguidos, meses
	#     cerrados en total, postura de Rosas (0 abierto)
	set_global_variable = { name = gg_plata_months value = 0 }
	# EN/ES: the war journal's sources, this month and last / las fuentes de la balanza, este mes y el pasado
	gg_war_sources_reset = yes
	set_global_variable = { name = gg_plata_closed_streak value = 0 }
	set_global_variable = { name = gg_plata_closed_total value = 0 }
	set_global_variable = { name = gg_plata_rosas_stance value = 0 }
	gg_plata_rosas_stance_apply = yes
	# EN/ES: Magellan has no forts in 1836: its banks hold the strait by modifier
	#     / Magallanes no tiene fuertes en 1836: sus orillas lo controlan por modificador
	p:x5A7430.state ?= { add_modifier = { name = gg_strait_control_magellan } }
	p:x5BA7C2.state ?= { add_modifier = { name = gg_strait_control_magellan } }
	# EN/ES: Codex review 2026-09-19 -- half of a government's ground in the other's hands, written
	#     down monthly while the war lasts / medio pais en manos del otro, anotado mes a mes
	set_global_variable = { name = gg_cerrito_overrun value = 0 }
	set_global_variable = { name = gg_defensa_overrun value = 0 }
	# EN/ES: months in a row with the war support of each government at -25 or worse / meses seguidos
	#     con el apoyo de cada gobierno en -25 o peor
	set_global_variable = { name = gg_cerrito_exhausted value = 0 }
	set_global_variable = { name = gg_defensa_exhausted value = 0 }
	set_global_variable = { name = guerra_grande_colorado_score value = 0 }
}

# EN: usage: gg_score_blanco = { AMOUNT = 2 }. AMOUNT must be a positive literal: the colorado
#     side's tooltip key is built from it, so a negative number or a named value would not resolve.
#     To take score away from one side, give it to the other. Moves the score, and the journal bar if the
#     journal already exists (before Carpinteria it does not; .1 syncs the bar when it opens).
# ES: uso: gg_score_blanco = { AMOUNT = 2 }. AMOUNT tiene que ser un numero positivo escrito tal
#     cual: la clave del tooltip se arma con el. Para restarle a un bando, se le suma
#     al otro. Mueve el puntaje, y la barra si el journal ya existe
#     (antes de Carpinteria no existe; .1 sincroniza la barra al abrirlo).
gg_score_blanco = {
	# EN/ES: shown to the player; global variables have no tooltip of their own / se muestra al jugador
	custom_tooltip = {
		text = gg_tt_score_blanco_$AMOUNT$
		change_global_variable = { name = guerra_grande_blanco_score add = $AMOUNT$ }
		change_global_variable = { name = gg_src_decisions_blanco add = $AMOUNT$ }
		je:je_guerra_grande ?= {
			set_bar_progress = { name = guerra_grande_war_score_bar value = gg_war_score_bar_value }
		}
	}
}

gg_score_colorado = {
	# EN/ES: shown to the player; global variables have no tooltip of their own / se muestra al jugador
	custom_tooltip = {
		text = gg_tt_score_colorado_$AMOUNT$
		change_global_variable = { name = guerra_grande_colorado_score add = $AMOUNT$ }
		change_global_variable = { name = gg_src_decisions_colorado add = $AMOUNT$ }
		je:je_guerra_grande ?= {
			set_bar_progress = { name = guerra_grande_war_score_bar value = gg_war_score_bar_value }
		}
	}
}

# EN: usage: gg_patron_change = { PATRON = francia DELTA = -15 }
#     PATRON is one of rosas, francia, britania, brasil, urquiza. The result is kept in 0-100.
# ES: uso: gg_patron_change = { PATRON = francia DELTA = -15 }
#     PATRON es rosas, francia, britania, brasil o urquiza. El resultado queda entre 0 y 100.
gg_patron_change = {
	# EN/ES: shown to the player; global variables have no tooltip of their own / se muestra al jugador
	custom_tooltip = {
		text = gg_tt_patron_$PATRON$_$DELTA$
		change_global_variable = { name = gg_patron_$PATRON$ add = $DELTA$ }
		if = {
			limit = { global_var:gg_patron_$PATRON$ > 100 }
			set_global_variable = { name = gg_patron_$PATRON$ value = 100 }
		}
		else_if = {
			limit = { global_var:gg_patron_$PATRON$ < 0 }
			set_global_variable = { name = gg_patron_$PATRON$ value = 0 }
		}
		debug_log = "[TimeKeeper.GetCurrentDate.GetString]: GG_PATRON $PATRON$ $DELTA$"
	}
	# EN/ES: 22/9/2026 -- what this patron's backing buys, written out once for every call. The preview
	#     cannot show the level after the change, so the text gives the thresholds, not a level (Codex)
	#     / lo que compra el respaldo de este padrino; da los umbrales, no un nivel
	# EN/ES: before the split there is no Defensa or Cerrito yet / antes de la particion no hay Defensa ni Cerrito
	if = {
		limit = { has_global_variable = guerra_grande_split_done }
		custom_tooltip = gg_tt_patron_$PATRON$_means
	}
	else = {
		custom_tooltip = gg_tt_patron_$PATRON$_means_pre
	}
	# EN/ES: a change of level is announced at once, not a month later / el cambio de nivel se avisa ya
	hidden_effect = { gg_patron_level_watch_$PATRON$ = yes }
}

# EN: T6, the Defensa's law of 1842, dispatched once (Codex review 2026-09-19). Three places could
#     schedule it -- the dated dispatcher of 12 December, Arroyo Grande six days later, and the
#     partition's backstop -- and all three asked the same question: "is it settled yet?". A popup
#     waits 1825 days for an answer, so an unanswered law was not settled and a second copy could be
#     scheduled on top of the first. Dispatched and settled are two different things now.
# ES: T6, la ley de la Defensa de 1842, despachada una sola vez. Tres lugares podian agendarla -- el
#     despachador del 12 de diciembre, Arroyo Grande seis dias despues y el respaldo de la particion
#     -- y los tres preguntaban lo mismo: "ya esta resuelta?". Un popup espera 1825 dias una
#     respuesta, asi que una ley sin contestar no estaba resuelta y se podia agendar una segunda
#     copia encima de la primera. Despachada y resuelta ya no son lo mismo.
gg_abolition_dispatch = {
	if = {
		limit = {
			NOT = { has_global_variable = gg_abolition_dispatched }
			# EN/ES: the law lives on the Defensa, not on whoever calls this / la ley vive en la
			#     Defensa, no en quien llame a este efecto
			c:URU ?= { NOT = { has_variable = guerra_grande_abolition_settled } }
		}
		set_global_variable = gg_abolition_dispatched
		c:URU ?= {
			trigger_event = { id = gg_abolition.2 days = $DAYS$ popup = yes }
		}
	}
}

# EN: T7, the diplomatic table is reserved while a chain of the powers is running (Codex review
#     2026-09-19). The Hood bases schedule Montevideo's answer 29 days later, and the long popups of
#     Gore-Gros and the Larraniaga funeral can sit open for years: in all that time nothing stopped
#     the weekly arbiter from opening a different peace, so the player could end up with two peace
#     menus on the table at once. gg_peace_offered cannot do this job, because the chain has not
#     offered anything yet: it is still negotiating.
# ES: T7, la mesa diplomatica queda reservada mientras corre una cadena de las potencias. Las bases
#     Hood agendan la respuesta de Montevideo 29 dias despues, y los popups largos de Gore-Gros y las
#     exequias pueden quedar abiertos anios: en todo ese tiempo nada impedia que el arbitro semanal
#     abriera otra paz, y el jugador podia terminar con dos menus de paz a la vez. gg_peace_offered no
#     sirve para esto, porque la cadena todavia no ofrecio nada: esta negociando.
gg_peace_chain_hold = {
	set_global_variable = gg_peace_chain_pending
}

gg_peace_chain_clear = {
	if = {
		limit = { has_global_variable = gg_peace_chain_pending }
		remove_global_variable = gg_peace_chain_pending
	}
}

# EN/ES: Codex review 2026-09-19 -- gg_victory_pending was set when the victory menu was scheduled and
#     nobody ever took it off, so the menu's own trigger stayed true for the rest of the game. These
#     three are answers to a question that has been answered. / gg_victory_pending se prendia al
#     agendar el menu de la victoria y nadie la apagaba nunca, asi que el trigger del menu quedaba
#     verdadero el resto de la partida. Las tres son respuestas a una pregunta ya contestada.
gg_victory_clear = {
	if = { limit = { has_global_variable = gg_victory_pending } remove_global_variable = gg_victory_pending }
	if = { limit = { has_global_variable = gg_victory_third_party } remove_global_variable = gg_victory_third_party }
	if = { limit = { has_global_variable = gg_victory_timeout } remove_global_variable = gg_victory_timeout }
}

# EN: T8, the postwar, started from one place and only once (Codex review 2026-09-19). It used to
#     hang off the closing, inside the guard "no peace has been signed yet" -- so every negotiated
#     peace, which signs itself, skipped it: no postwar at all, and a Cerrito that won at the table
#     never got the Republic's tag back. It is called now by the peace menus and by the victory menu,
#     after the player has answered, so the postwar knows what was decided.
# ES: T8, la posguerra, arrancada desde un solo lugar y una sola vez. Colgaba del cierre, adentro de
#     la guarda "todavia nadie firmo la paz", asi que toda paz negociada -- que se firma a si misma --
#     se la saltaba: no habia posguerra, y un Cerrito que ganaba en la mesa no recuperaba el tag de la
#     Republica. Ahora la llaman los menus de paz y el de la victoria, despues de que el jugador
#     contesto, asi la posguerra sabe que se decidio.
gg_postwar_start = {
	if = {
		limit = {
			NOT = { has_global_variable = gg_postwar_started }
			NOT = { has_global_variable = gg_reincorporated }
		}
		set_global_variable = gg_postwar_started
		gg_wartime_modifiers_clear = yes
		gg_postwar_dispatch = yes
	}
}

# EN: THE SIEGE MODIFIERS OUTLIVED THE SIEGE (balance audit, Codex, 19 September 2026). All four are
#     applied with years = 15 from 1843, and nothing ever took them off: a peace in 1851 left the
#     plaza still walled, the countryside still without a port and the Cerrito still without
#     breaching guns until 1858. Seven years of a war that ended. It matters more now that these
#     carry the rebalanced numbers -- Montevideo's extra working adults are a besieged city arming
#     its whole population, which is exactly the thing that has to stop when the siege lifts.
#     Written as its own effect, called once from gg_postwar_start, which is the single idempotent
#     door every ending goes through (the peace menu's four options and the closing dispatch).
#     Guarded one by one with has_modifier so nothing is removed from a country that never had it.
# ES: LOS MODIFICADORES DEL SITIO DURABAN MAS QUE EL SITIO. Los cuatro se aplican con years = 15
#     desde 1843 y nadie se los sacaba nunca: una paz en 1851 dejaba la plaza todavia amurallada, la
#     campania todavia sin puerto y al Cerrito todavia sin canones de brecha hasta 1858. Siete anios
#     de una guerra terminada. Importa mas ahora que llevan los numeros rebalanceados: los adultos
#     empleables de mas en Montevideo son una ciudad sitiada armando a toda su poblacion, que es
#     justo lo que tiene que terminar cuando se levanta el sitio.
#     Va como efecto propio, llamado una sola vez desde gg_postwar_start, que es la unica puerta
#     idempotente por la que pasan todos los finales (las cuatro opciones del menu de paz y el
#     despacho de cierre). Cada uno con su guarda de has_modifier, asi no se le saca nada a un pais
#     que nunca lo tuvo.
gg_wartime_modifiers_clear = {
	# EN: C10 (22/9/2026) -- the blockade of Montevideo was a siege measure: with the war over, Rosas
	#     opens the river (otherwise his AI would keep the 1843 closure for good).
	# ES: C10 -- el bloqueo de Montevideo era una medida del sitio: terminada la guerra, Rosas abre el
	#     rio (si no, su IA conservaria para siempre el cierre de 1843).
	if = {
		limit = { has_global_variable = gg_plata_rosas_stance }
		set_global_variable = { name = gg_plata_rosas_stance value = 0 }
		gg_plata_rosas_stance_apply = yes
	}
	every_country = {
		limit = {
			OR = {
				has_modifier = gg_siege_no_breaching_guns
				has_modifier = guerra_grande_walls_of_montevideo
				has_modifier = gg_siege_guns_of_the_plaza
				has_modifier = gg_siege_mortgaged_customs
				has_modifier = gg_siege_customs_house_at_the_buceo
				has_modifier = gg_siege_trading_with_the_enemy
				has_modifier = gg_siege_the_sea_lane_stays_open
				has_modifier = guerra_grande_montevideo_resupplied
				has_modifier = guerra_grande_river_plate_blockade
			}
		}
		if = {
			limit = { has_modifier = gg_siege_no_breaching_guns }
			remove_modifier = gg_siege_no_breaching_guns
		}
		if = {
			limit = { has_modifier = guerra_grande_walls_of_montevideo }
			remove_modifier = guerra_grande_walls_of_montevideo
		}
		if = { limit = { has_modifier = gg_siege_guns_of_the_plaza } remove_modifier = gg_siege_guns_of_the_plaza }
		if = { limit = { has_modifier = gg_siege_mortgaged_customs } remove_modifier = gg_siege_mortgaged_customs }
		if = { limit = { has_modifier = gg_siege_customs_house_at_the_buceo } remove_modifier = gg_siege_customs_house_at_the_buceo }
		if = { limit = { has_modifier = gg_siege_trading_with_the_enemy } remove_modifier = gg_siege_trading_with_the_enemy }
		if = { limit = { has_modifier = gg_siege_the_sea_lane_stays_open } remove_modifier = gg_siege_the_sea_lane_stays_open }
		if = { limit = { has_modifier = guerra_grande_montevideo_resupplied } remove_modifier = guerra_grande_montevideo_resupplied }
		if = { limit = { has_modifier = guerra_grande_river_plate_blockade } remove_modifier = guerra_grande_river_plate_blockade }
	}
	# EN/ES: Fleet state lives on Uruguay and must be cleared even when it has no country modifier.
	# El estado de la escuadra vive en Uruguay y se limpia aunque no tenga modificadores de pais.
	c:URU ?= {
		# EN/ES: 22/9 -- France's supply of the lent squadron ends with it / el sosten frances termina con la escuadra
		if = { limit = { has_modifier = gg_lent_squadron_french_supply } remove_modifier = gg_lent_squadron_french_supply }
		if = {
			limit = { has_variable = gg_lent_squadron_ref exists = var:gg_lent_squadron_ref }
			var:gg_lent_squadron_ref ?= {
				if = { limit = { exists = c:FRA } every_scope_ship = { set_ship_owner_multiple = c:FRA } }
			}
			# EN/ES: S02, Codex review 22/9 -- finished on the receiving country, as vanilla does; it was
			#     called from the formation scope / se cierra en el pais receptor; se llamaba desde la formacion
			c:FRA ?= { clear_ownership_transfer_fleet = yes }
		}
		if = { limit = { has_variable = gg_lent_squadron } remove_variable = gg_lent_squadron }
		if = { limit = { has_variable = gg_lent_squadron_ref } remove_variable = gg_lent_squadron_ref }
	}
	every_state = {
		limit = {
			OR = {
				has_modifier = gg_siege_port_without_a_countryside
				has_modifier = gg_siege_countryside_without_a_port
				has_modifier = gg_siege_the_land_frontier_stays_open
				has_modifier = gg_naval_anglo_french_station
			}
		}
		if = {
			limit = { has_modifier = gg_siege_port_without_a_countryside }
			remove_modifier = gg_siege_port_without_a_countryside
		}
		if = {
			limit = { has_modifier = gg_siege_countryside_without_a_port }
			remove_modifier = gg_siege_countryside_without_a_port
		}
		if = { limit = { has_modifier = gg_siege_the_land_frontier_stays_open } remove_modifier = gg_siege_the_land_frontier_stays_open }
		if = { limit = { has_modifier = gg_naval_anglo_french_station } remove_modifier = gg_naval_anglo_french_station }
	}
	# EN/ES: 23/9 -- the siege army's supply ends with the siege, whatever tag it wears now / termina con el sitio
	every_country = {
		limit = { has_modifier = gg_cerrito_lives_off_the_land }
		remove_modifier = gg_cerrito_lives_off_the_land
	}
	gg_calendar_close_wartime_relays = yes
}

# EN: Relays that require the war or both governments stop retrying after settlement. University
#     and the independent European withdrawals retain their postwar delivery.
# ES: Los relevos que requieren guerra o ambos gobiernos dejan de reintentarse tras la paz. La
#     Universidad y los retiros europeos independientes conservan su entrega de posguerra.
gg_calendar_close_wartime_relays = {
	set_global_variable = gg_cal_done_1
	set_global_variable = gg_cal_done_2
	set_global_variable = gg_cal_done_3
	set_global_variable = gg_cal_done_4
	set_global_variable = gg_cal_done_5
	set_global_variable = gg_cal_done_6
	set_global_variable = gg_cal_done_7
	set_global_variable = gg_cal_done_8
	set_global_variable = gg_cal_done_9
	set_global_variable = gg_cal_done_10
	set_global_variable = gg_cal_done_11
	set_global_variable = gg_cal_done_12
	set_global_variable = gg_cal_done_13
	set_global_variable = gg_cal_done_14
	set_global_variable = gg_cal_done_15
	set_global_variable = gg_cal_done_16
	set_global_variable = gg_cal_done_17
	set_global_variable = gg_cal_done_18
	set_global_variable = gg_cal_done_19
	set_global_variable = gg_cal_done_20
	set_global_variable = gg_cal_done_21
	set_global_variable = gg_cal_done_23
	set_global_variable = gg_cal_done_24
	set_global_variable = gg_cal_done_26
	set_global_variable = gg_cal_done_27
	set_global_variable = gg_cal_done_28
	set_global_variable = gg_cal_done_29
	set_global_variable = gg_cal_done_30
	set_global_variable = gg_cal_done_31
	set_global_variable = gg_cal_done_33
	set_global_variable = gg_cal_done_34
}

# EN: T1/T9, the closing of the war, in one idempotent place (Codex review 2026-09-19). It used to
#     live inside on_complete_all_involved only, and the journal has two ways out: complete and fail.
#     A foreign power holding Montevideo is a fail, so that game closed the war without handing the
#     final event to anybody -- while the mirror case (a third country eating the Cerrito) did get
#     one. Both hooks call this now, the mark makes it run once, and it always names a recipient.
# ES: T1/T9, el cierre de la guerra, en un solo lugar y una sola vez. Vivia dentro de
#     on_complete_all_involved nada mas, y el journal tiene dos salidas: complete y fail. Que una
#     potencia extranjera se quede con Montevideo es un fail, asi que esa partida cerraba la guerra
#     sin entregarle el evento final a nadie, mientras que el caso espejo (un tercero se come al
#     Cerrito) si lo recibia. Ahora lo llaman las dos salidas, la marca hace que corra una vez, y
#     siempre hay destinatario.
gg_war_closing_dispatch = {
	if = {
		limit = {
			NOT = { has_global_variable = gg_peace_settled }
			# EN/ES: Codex review 2026-09-19 -- with both governments gone there is nobody to hand the
			#     terms to, and marking the peace as pending would leave a menu waiting forever. The
			#     journal still closes: this only decides who signs. / sin los dos gobiernos no hay a
			#     quien entregarle el pliego, y marcar la paz como pendiente dejaba un menu esperando
			#     para siempre. El journal cierra igual: esto solo decide quien firma.
			OR = {
				exists = c:CER
				exists = c:URU
			}
		}
		set_global_variable = gg_peace_settled
		set_global_variable = gg_victory_pending
		# EN: a third country took the war away from both (Codex review 2026-09-19, second pass: the
		#     simplified test lost the mirror case -- a foreign power eating the Cerrito while the
		#     Defensa still held Montevideo was read as the Defensa winning the war on its own).
		#     Three ways: nobody oriental holds the capital, or one government is gone and the other
		#     does not hold the ground that government lost.
		# ES: un tercero les saco la guerra a los dos. La prueba simplificada perdia el caso espejo:
		#     una potencia extranjera comiendose al Cerrito con la Defensa todavia en Montevideo se
		#     leia como que la Defensa habia ganado la guerra sola. Tres formas: que ningun oriental
		#     tenga la capital, o que falte un gobierno y el otro no tenga el suelo que ese perdio.
		if = {
			limit = {
				OR = {
					NOR = {
						c:URU ?= { any_scope_state = { state_region = s:STATE_MONTEVIDEO } }
						c:CER ?= { any_scope_state = { state_region = s:STATE_MONTEVIDEO } }
					}
					AND = {
						NOT = { exists = c:CER }
						c:URU ?= { NOT = { any_scope_state = { state_region = s:STATE_SAN_JOSE } } }
					}
					AND = {
						NOT = { exists = c:URU }
						c:CER ?= { NOT = { any_scope_state = { state_region = s:STATE_MONTEVIDEO } } }
					}
				}
			}
			set_global_variable = gg_victory_third_party
			# EN/ES: nobody oriental won this one, so the occupation marks must not say otherwise
			#     / no gano ningun oriental, asi que las marcas de ocupacion no pueden decir otra cosa
			set_global_variable = { name = gg_cerrito_overrun value = 0 }
			set_global_variable = { name = gg_defensa_overrun value = 0 }
		}
		else_if = {
			limit = {
				NOR = {
					gg_cerrito_won_militarily = yes
					gg_defensa_won_militarily = yes
				}
			}
			set_global_variable = gg_victory_timeout
		}
		# EN/ES: who writes the terms / quien escribe el pliego
		# EN: Codex review 2026-09-19, third pass -- a war that a third country ended does not have an
		#     oriental winner, and asking the military triggers for one produced answers like "the
		#     Cerrito won" while a foreign flag flew over Montevideo. There the paper goes to whoever
		#     is left standing, the Defensa first because the Republic's tag is hers, and gg_peace.5
		#     already has its own text for this ending.
		# ES: una guerra que termino un tercero no tiene ganador oriental, y preguntarle a los triggers
		#     militares daba respuestas como "gano el Cerrito" con una bandera extranjera sobre
		#     Montevideo. Ahi el papel es del que haya quedado en pie, la Defensa primero porque el tag
		#     de la Republica es suyo, y gg_peace.5 ya tiene su texto para este final.
		if = {
			limit = {
				has_global_variable = gg_victory_third_party
				exists = c:URU
			}
			random_country = {
				limit = { has_variable = guerra_grande_defensa_government }
				trigger_event = { id = gg_peace.5 days = 1 popup = yes }
			}
		}
		else_if = {
			limit = { has_global_variable = gg_victory_third_party }
			random_country = {
				limit = { has_variable = guerra_grande_cerrito_government }
				trigger_event = { id = gg_peace.5 days = 1 popup = yes }
			}
		}
		else_if = {
			limit = { gg_cerrito_won_militarily = yes }
			random_country = {
				limit = { has_variable = guerra_grande_cerrito_government }
				trigger_event = { id = gg_peace.5 days = 1 popup = yes }
			}
		}
		else_if = {
			limit = { gg_defensa_won_militarily = yes }
			random_country = {
				limit = { has_variable = guerra_grande_defensa_government }
				trigger_event = { id = gg_peace.5 days = 1 popup = yes }
			}
		}
		# EN/ES: no winner (the 1860 cut, or a stalemate): the paper is read by whoever is ahead on the
		#     same number the bar shows, and if nobody is ahead, by the Defensa / sin ganador (el corte
		#     de 1860 o un empate): lee el papel el que va arriba en el mismo numero que muestra la
		#     barra, y si no hay nadie arriba, la Defensa
		else_if = {
			limit = {
				exists = c:CER
				gg_war_balance_blanco > 0
			}
			random_country = {
				limit = { has_variable = guerra_grande_cerrito_government }
				trigger_event = { id = gg_peace.5 days = 1 popup = yes }
			}
		}
		else_if = {
			limit = { exists = c:URU }
			random_country = {
				limit = { has_variable = guerra_grande_defensa_government }
				trigger_event = { id = gg_peace.5 days = 1 popup = yes }
			}
		}
		else_if = {
			limit = { exists = c:CER }
			random_country = {
				limit = { has_variable = guerra_grande_cerrito_government }
				trigger_event = { id = gg_peace.5 days = 1 popup = yes }
			}
		}
	}
	# EN: Codex review 2026-09-19 -- the postwar lives OUTSIDE the "no peace signed" guard, and it is
	#     not started here when there is a victory menu on the table: gg_peace.5 starts it from its
	#     own options. Otherwise the postwar chose its branch (and changed the Cerrito's tag, which
	#     can eat the pending event) before the player had answered the menu.
	# ES: la posguerra va AFUERA de la guarda de "nadie firmo la paz", y no arranca aca cuando hay un
	#     menu de victoria sobre la mesa: lo arranca gg_peace.5 desde sus propias opciones. Si no, la
	#     posguerra elegia rama -- y le cambiaba el tag al Cerrito, lo que puede comerse el evento
	#     pendiente -- antes de que el jugador contestara.
	if = {
		limit = { NOT = { has_global_variable = gg_victory_pending } }
		gg_postwar_start = yes
	}
}

# EN: T3b/FN-1, the Cerrito's two mechanics, taken out of the buttons (Codex review 2026-09-19). The
#     montonera levy and the river blockade lived inside scripted buttons, and the AI never presses a
#     button: with the player on the Defensa, the Cerrito never raised its horsemen, never called
#     Brown's squadron and never declared the blockade, so the sea stayed open by itself and the
#     siege -- the heart of that side's war -- did nothing at all. The code is the same; now it is an
#     effect the button calls, and so can a hidden event for the AI.
# ES: T3b/FN-1, las dos mecanicas del Cerrito, sacadas de los botones. La leva de montoneras y el
#     bloqueo del rio vivian adentro de botones, y la IA no aprieta botones nunca: con el jugador en
#     la Defensa, el Cerrito no levantaba sus jinetes, no llamaba a la escuadra de Brown y no
#     declaraba el bloqueo, asi que el mar quedaba abierto solo y el sitio -- el corazon de la guerra
#     de ese bando -- no hacia nada. El codigo es el mismo; ahora es un efecto que llama el boton, y
#     que tambien puede llamar un evento oculto para la IA.

gg_montonera_levy = {
		# EN: visible counter, since debug_log writes nothing in this release build
		#     (verified: not even vanilla's own debug_log lines appear in game.log).
		#     Read from the journal's status text.
		# ES: contador visible, porque debug_log no escribe nada en este build de release
		#     (verificado: ni los debug_log propios de vanilla aparecen en game.log). Se lee
		#     desde el texto de estado del journal.
		# EN: self-healing guard -- this fix landed after some saves already ran
		#     on_game_started once, so the global init there never fires for them.
		# ES: guarda auto-reparable -- este arreglo llego despues de que algunas
		#     partidas ya corrieron on_game_started una vez, asi que la inicializacion
		#     de alla nunca les llega.
		set_variable = guerra_grande_montonera_levy_raised
		# EN/ES: T3b, FE-9 -- the caudillos' levy: the balance tilts, the till pays
		add_treasury = -10000
		# EN: cavalry first and most of it. This is the one thing the besiegers had that the city
		#     could never buy: men who came with their own horses.
		# ES: caballeria primero y casi todo. Es lo unico que tenian los sitiadores que la ciudad
		#     no podia comprar: hombres que venian con caballo propio.
		gg_select_formation_hq_state = yes
		# EN: Without an owned state there is no valid HQ; skip this formation.
		# ES: Sin un estado propio no hay cuartel valido; se omite esta formacion.
		if = {
			limit = { exists = scope:gg_formation_hq_state }
			create_military_formation = {
				type = army
				hq_region = scope:gg_formation_hq_state.region
				combat_unit = {
					type = unit_type:combat_unit_type_hussars
					state_region = s:STATE_SAN_JOSE
					count = 3
				}
				combat_unit = {
					type = unit_type:combat_unit_type_irregular_infantry
					state_region = s:STATE_SAN_JOSE
					count = 1
				}
			}
		}
		# EN: this modifier is already the mod's name for what the levy renews -- caudillos,
		#     estancias and the parish militias. If the Cerrito already carries it from the
		#     faction choice, applying it again refreshes the duration rather than stacking, which
		#     is the intended reading: the countryside is called on a second time.
		# ES: este modificador ya es el nombre que el mod le da a lo que renueva la leva:
		#     caudillos, estancias y milicias de parroquia. Si el Cerrito ya lo trae de la eleccion
		#     de bando, aplicarlo de nuevo renueva la duracion en vez de acumularse, que es la
		#     lectura buscada: se llama a la campania por segunda vez.
		add_modifier = {
			name = guerra_grande_blanco_order
			years = 10
			is_decaying = yes
		}
		# EN: and this is the price. Every man on a horse is a man off the estancia; the levy is
		#     paid for by the rural economy, not by a customs house.
		# ES: y este es el precio. Cada hombre a caballo es un hombre menos en la estancia; la leva
		#     la paga la economia rural, no una aduana.
		add_radicals = {
			value = medium_radicals
			strata = lower
		}
	}

gg_river_blockade = {
		# EN: visible counter, since debug_log writes nothing in this release build
		#     (verified: not even vanilla's own debug_log lines appear in game.log).
		#     Read from the journal's status text.
		# ES: contador visible, porque debug_log no escribe nada en este build de release
		#     (verificado: ni los debug_log propios de vanilla aparecen en game.log). Se lee
		#     desde el texto de estado del journal.
		# EN: self-healing guard -- this fix landed after some saves already ran
		#     on_game_started once, so the global init there never fires for them.
		# ES: guarda auto-reparable -- este arreglo llego despues de que algunas
		#     partidas ya corrieron on_game_started una vez, asi que la inicializacion
		#     de alla nunca les llega.
		set_variable = guerra_grande_river_blockade_asked
		# EN: Rosas' backing comes with the squadron: money, cattle and veterans.
		# ES: el apoyo de Rosas viene con la escuadra: plata, ganado y veteranos.
		add_modifier = {
			name = guerra_grande_rosista_support
			years = 8
			is_decaying = yes
		}
		# EN: C10 (22/9/2026) -- the blockade is Rosas's, not the Cerrito's: Oribe has no bank (his port
		#     is the Buceo). He asks, and Rosas shuts his bank of the river: an AI Rosas at once, a
		#     human Rosas gets the request (gg_plata.2) and closes it himself. No modifier on the city:
		#     the engine cuts its trade. Historically announced 9 March, in force 1 April 1843.
		# ES: C10 -- el bloqueo es de Rosas, no del Cerrito: Oribe no tiene orilla (su puerto es el
		#     Buceo). Oribe pide, y Rosas cierra su orilla: la IA en el acto, un Rosas humano recibe el
		#     pedido y la cierra el. Ningun modificador sobre la ciudad: el motor le corta el comercio.
		c:ARG ?= {
			if = {
				limit = { is_player = yes }
				trigger_event = { id = gg_plata.2 popup = yes }
			}
			else = {
				gg_plata_rosas_close = yes
			}
		}
		# EN: FN-1 -- and the squadron itself. Rosas lent Brown with what the Confederation had on
		#     the river: brigs and schooners, three of them here as frigates, the smallest fleet the
		#     engine can float. Based on the coast the Cerrito holds, because Montevideo is the
		#     enemy's. Whether the AI puts it on a blockade mission is up to it (the engine has no
		#     effect to order a mission), so the declared blockade keeps counting as well.
		# ES: FN-1 -- y la escuadra. Rosas prestaba a Brown con lo que la Confederacion tenia en el
		#     rio: bergantines y goletas, aca tres fragatas, la flota mas chica que el motor sostiene.
		#     Con base en la costa que tiene el Cerrito, porque Montevideo es del enemigo. Que la IA
		#     la ponga en mision de bloqueo depende de ella (el motor no tiene efecto para ordenar
		#     una mision), asi que el bloqueo declarado sigue contando igual.
		if = {
			limit = { NOT = { has_variable = gg_brown_squadron_raised } }
			set_variable = gg_brown_squadron_raised
			# EN: 22/9/2026 -- one base for ships and crews. The squadron used to be born unmanned: a
			#     level-1 naval administration gives at most 1000 sailors (pm_simple_sailor_recruitment,
			#     workforce-scaled) while three frigates need 1500 (ship_crew_max_add = 500 each), and
			#     without Maldonado the administration went to a random coastal state while the ships
			#     were raised in the HQ state. Now one coastal state is chosen first -- Maldonado if the
			#     Cerrito holds it -- and it gets both the administration (level 2 = 2000 sailors) and
			#     the ships, with the administration built BEFORE the formation. The engine has no
			#     effect to man ships instantly, so the crews still fill in as the building hires.
			# ES: 22/9/2026 -- una sola base para barcos y dotaciones. La escuadra nacia sin gente: la
			#     administracion naval de nivel 1 da como mucho 1000 marineros (pm_simple_sailor_recruitment,
			#     escalado por empleo) y tres fragatas piden 1500 (ship_crew_max_add = 500 cada una), y
			#     sin Maldonado la administracion iba a un estado costero al azar mientras los barcos
			#     nacian en el estado del cuartel. Ahora se elige primero un estado costero -- Maldonado
			#     si lo tiene el Cerrito -- y ese recibe la administracion (nivel 2 = 2000 marineros) y
			#     los barcos, con la administracion creada ANTES de la formacion. El motor no tiene
			#     efecto para tripular al instante, asi que las dotaciones se llenan a medida que el
			#     edificio contrata.
			# EN: Playtest 2026-09-19 context kept: vanilla never gives Uruguay a naval administration, and
			#     a lent squadron comes with its base and its men -- Brown's crews were Argentine.
			# ES: contexto de la prueba del 19/9: vanilla nunca le da al Uruguay administracion naval, y
			#     una escuadra prestada viene con su base y su gente -- las dotaciones de Brown eran argentinas.
			clear_saved_scope = gg_brown_base_state
			clear_saved_scope = gg_brown_fleet
			if = {
				limit = { exists = s:STATE_MALDONADO.region_state:CER }
				s:STATE_MALDONADO.region_state:CER = { save_scope_as = gg_brown_base_state }
			}
			else_if = {
				limit = {
					any_scope_state = {
						owner = prev
						is_coastal = yes
					}
				}
				random_scope_state = {
					limit = {
						owner = prev
						is_coastal = yes
					}
					save_scope_as = gg_brown_base_state
				}
			}
			# EN: Without an owned coastal state there is no port to raise ships in; skip the squadron.
			# ES: Sin un estado costero propio no hay puerto donde armar barcos; se omite la escuadra.
			if = {
				limit = { exists = scope:gg_brown_base_state }
				scope:gg_brown_base_state = {
					# EN: after a lost squadron (gg_powers.11) the base may already stand; do not stack it.
					# ES: tras perder la escuadra (gg_powers.11) la base puede seguir en pie; no se duplica.
					if = {
						limit = { NOT = { has_building = building_naval_administration } }
						create_building = {
							building = building_naval_administration
							level = 2
						}
					}
				}
				create_military_formation = {
					type = fleet
					# EN/ES: Codex review 2026-09-19 -- "Cannot find naval HQ for Cerrito Government in
					#     region_la_plata": the HQ must be a region where the Cerrito owns a state. The base
					#     state is owned by definition. / el cuartel tiene que estar en una region con un
					#     estado del Cerrito; el estado base lo es por definicion.
					hq_region = scope:gg_brown_base_state.region
					name = gg_brown_squadron_name
					save_scope_as = gg_brown_fleet
					ship = {
						type = ship_type:ship_type_frigate
						state_region = scope:gg_brown_base_state.state_region
						count = 3
					}
				}
				set_variable = { name = gg_brown_squadron_ref value = scope:gg_brown_fleet }
			}
			# EN: 22/9/2026 -- and the man himself, the real one. Guillermo Brown commanded the squadron
			#     that blockaded Montevideo; Rosas lent him to Oribe's blockade. create_character used to
			#     make a SECOND Brown while the original kept commanding the Armada_Argentina. Now the
			#     existing Brown is found (the Cerrito's, then Argentina's, then any country, then exile), taken
			#     off his formation, transferred to the Cerrito and given the squadron. A new one is
			#     created only if none exists anywhere. Side effect, accepted: Argentina's fleet is left
			#     without an admiral until the engine or the player names another -- which is what the
			#     loan meant.
			# ES: 22/9/2026 -- y el hombre, el verdadero. Guillermo Brown mandaba la escuadra que bloqueo
			#     Montevideo; Rosas se lo presto al bloqueo de Oribe. create_character creaba un SEGUNDO
			#     Brown mientras el original seguia al mando de la Armada_Argentina. Ahora se busca al
			#     Brown que existe (el del Cerrito, despues el de la Argentina, despues cualquier pais,
			#     despues los exiliados), se lo saca de su formacion, se lo pasa al Cerrito y se le da la escuadra.
			#     Solo se crea uno si no hay ninguno. Efecto aceptado: la flota argentina queda sin
			#     almirante hasta que el motor o el jugador nombren otro -- que es lo que era el prestamo.
			if = {
				limit = { exists = scope:gg_brown_fleet }
				save_scope_as = gg_brown_receiver
				clear_saved_scope = gg_brown_admiral
				clear_saved_scope = gg_brown_already_here
				# EN: the Cerrito's own Brown first (a squadron raised again after gg_powers.11), then Rosas's.
				# ES: primero el Brown que ya tenga el Cerrito (escuadra rearmada tras gg_powers.11), despues el de Rosas.
				random_scope_character = {
					limit = {
						is_character_alive = yes
						has_template = ARG_William_Brown
					}
					save_scope_as = gg_brown_admiral
					save_scope_as = gg_brown_already_here
				}
				if = {
					limit = { NOT = { exists = scope:gg_brown_admiral } }
					c:ARG ?= {
						random_scope_character = {
							limit = {
								is_character_alive = yes
								has_template = ARG_William_Brown
							}
							save_scope_as = gg_brown_admiral
						}
					}
				}
				if = {
					limit = { NOT = { exists = scope:gg_brown_admiral } }
					random_country = {
						limit = {
							any_scope_character = {
								is_character_alive = yes
								has_template = ARG_William_Brown
							}
						}
						random_scope_character = {
							limit = {
								is_character_alive = yes
								has_template = ARG_William_Brown
							}
							save_scope_as = gg_brown_admiral
						}
					}
				}
				if = {
					limit = { NOT = { exists = scope:gg_brown_admiral } }
					random_character_in_exile_pool = {
						limit = { has_template = ARG_William_Brown }
						save_scope_as = gg_brown_admiral
					}
				}
				if = {
					limit = { exists = scope:gg_brown_admiral }
					scope:gg_brown_admiral = {
						if = {
							limit = { exists = this.commander_military_formation }
							unassign_from_formation = yes
						}
						if = {
							limit = { is_in_exile_pool = yes }
							transfer_character = scope:gg_brown_receiver
							# EN/ES: vanilla's own cleanup when a character leaves exile
							#     / la limpieza de vanilla cuando un personaje sale del exilio
							free_from_exile_effect = yes
						}
						else_if = {
							limit = { NOT = { exists = scope:gg_brown_already_here } }
							transfer_character = scope:gg_brown_receiver
						}
						# EN: an exiled character is stripped to agitator; give the admiral role back.
						# ES: al exiliado le dejan solo el rol de agitador; se le devuelve el de almirante.
						if = {
							limit = { NOT = { has_role = character_role_admiral } }
							replace_character_roles = { add = { admiral } remove_all = yes }
						}
					}
				}
				else = {
					create_character = {
						template = ARG_William_Brown
						commander_rank = commander_rank_3
						save_scope_as = gg_brown_admiral
					}
				}
				scope:gg_brown_admiral = {
					transfer_to_formation = scope:gg_brown_fleet
				}
				# EN: insurance for saves where the old create_character already made a second Brown: keep
				#     the one just given the squadron and remove every other living copy (the same sweep
				#     as gg_return_rivera; not gg_deduplicate_character_worldwide, whose random pick could
				#     remove the admiral just placed).
				# ES: seguro para partidas donde el create_character viejo ya hizo un segundo Brown: se
				#     queda el que recien recibio la escuadra y se quita cualquier otra copia viva (el mismo
				#     barrido que gg_return_rivera; no gg_deduplicate_character_worldwide, cuya eleccion al
				#     azar podria quitar al almirante recien puesto).
				every_country = {
					every_scope_character = {
						limit = {
							is_character_alive = yes
							has_template = ARG_William_Brown
							NOT = { this = scope:gg_brown_admiral }
						}
						kill_character = yes
					}
				}
				clear_saved_scope = gg_brown_already_here
				clear_saved_scope = gg_brown_receiver
			}
		}
	}

# EN: T6/T7, the dated calendar's relay (events/guerra_grande_calendar.txt has the whole reasoning).
#     Hands a dated event to the government that owns it. If the country has not split yet the relay
#     waits a month and tries again, so a late partition does not swallow what was dated before it.
#     The mark makes it fire once, because two copies of every relay are scheduled (Rosas and the
#     Republic) against the chance that a tag change at the partition drops the queued events.
# ES: T6/T7, el relevo del calendario fechado (el razonamiento entero esta en
#     events/guerra_grande_calendar.txt). Le entrega un evento fechado al gobierno que le toca. Si el
#     pais todavia no se partio, el relevo espera un mes y reintenta, asi una particion tardia no se
#     come lo fechado antes. La marca hace que salga una sola vez, porque de cada relevo hay dos
#     copias agendadas (Rosas y la Republica) por si el cambio de tag de la particion se lleva puestos
#     los eventos agendados.
gg_cal_fire = {
	if = {
		limit = { NOT = { has_global_variable = $DONE$ } }
		if = {
			# EN: Only the recipient's immediate acknowledges delivery. Its trigger can reject
			#     this attempt even when the country exists; retry independently below.
			# ES: Solo el immediate del destinatario confirma la entrega. Su trigger puede
			#     rechazar el intento aunque exista el pais; el reintento de abajo es independiente.
			limit = {
				has_global_variable = guerra_grande_split_done
				any_country = { has_variable = $SIDE$ }
			}
			random_country = {
				limit = { has_variable = $SIDE$ }
				trigger_event = { id = $EVENT$ days = 0 popup = yes }
			}
		}
		if = {
			limit = {
				year < 1856
				NOT = { has_global_variable = $DONE$ }
			}
			trigger_event = { id = $RETRY$ days = 30 }
		}
	}
}

# EN/ES: the same, for the events that are not a popup / lo mismo, para los que no son popup
gg_cal_fire_quiet = {
	if = {
		limit = { NOT = { has_global_variable = $DONE$ } }
		if = {
			# EN/ES: same guard as gg_cal_fire above / la misma guarda que gg_cal_fire de arriba
			limit = {
				has_global_variable = guerra_grande_split_done
				any_country = { has_variable = $SIDE$ }
			}
			random_country = {
				limit = { has_variable = $SIDE$ }
				trigger_event = { id = $EVENT$ days = 0 }
			}
		}
		if = {
			limit = {
				year < 1856
				NOT = { has_global_variable = $DONE$ }
			}
			trigger_event = { id = $RETRY$ days = 30 }
		}
	}
}

# EN/ES: the same relay for what does not need two governments: the effect is global, so it runs on
#     whichever copy of the relay gets there first / el mismo relevo para lo que no necesita dos
#     gobiernos: el efecto es global, asi que corre en la copia que llegue primero
gg_cal_fire_here = {
	if = {
		limit = { NOT = { has_global_variable = $DONE$ } }
		trigger_event = { id = $EVENT$ days = 0 }
	}
}

# EN: Keep the existing peace-table guard and require a recipient before attempting delivery.
#     As above, only the recipient writes done; a rejected attempt keeps the monthly retry alive.
# ES: Conserva la guarda de la mesa de paz y exige destinatario antes de intentar la entrega.
#     Como arriba, solo el destinatario escribe done; un rechazo conserva el reintento mensual.
gg_cal_fire_if_table_free = {
	if = {
		limit = { NOT = { has_global_variable = $DONE$ } }
		if = {
			limit = {
				has_global_variable = guerra_grande_split_done
				any_country = { has_variable = $SIDE$ }
				# EN/ES: load test 2026-09-19 -- this used to arrive as a $CONDITION$ parameter, and the
				#     engine cannot compile a parameter that carries a trigger: "failed for unknown
				#     arguments: =, has_global_variable". The whole file stopped parsing from there, so
				#     twenty-one of the thirty-three relays did not exist. Written here, it compiles.
				#     / esto llegaba como parametro y el motor no puede compilar un parametro que trae
				#     un trigger adentro: el archivo dejaba de parsearse desde ahi y veintiun relevos de
				#     los treinta y tres no existian. Escrito aca, compila.
				NOT = { has_global_variable = gg_peace_offered }
			}
			random_country = {
				limit = { has_variable = $SIDE$ }
				trigger_event = { id = $EVENT$ days = 0 popup = yes }
			}
		}
		if = {
			limit = {
				year < 1856
				NOT = { has_global_variable = $DONE$ }
			}
			trigger_event = { id = $RETRY$ days = 30 }
		}
	}
}

# EN: T6, C18/FX-4. The whole post-split calendar, in one place, on absolute historical dates.
#     Called at the end of both guerra_grande.3 options, when both governments exist, and
#     addressed by tag so the Defensa and the Cerrito get their own events whoever the player is.
#     The split itself can move (F1-1 c brings Arroyo Grande a year forward, F1-4 b delays it six
#     months), so each variant carries its own delays. Anything whose date already passed when
#     the split happens fires the day after (delay 1). Recompute with the table in the T6 commit.
# ES: T6, C18/FX-4. Todo el calendario posterior a la particion, en un solo lugar y en fechas
#     historicas absolutas. Lo llaman las dos opciones de guerra_grande.3, cuando ya existen los
#     dos gobiernos, y va por tag para que cada uno reciba lo suyo sea quien sea el jugador. La
#     particion se puede mover (F1-1 c adelanta Arroyo Grande un anio, F1-4 b la atrasa seis
#     meses), asi que cada variante tiene sus demoras. Lo que ya paso al partirse dispara al dia
#     siguiente (demora 1).
gg_partition_calendar = {
	# EN/ES: T9 -- the new characters, once both governments exist / los personajes nuevos
	gg_spawn_war_characters = yes
	# EN/ES: the siege portrait has its defender from the first day / el retrato del sitio, desde el primer dia
	gg_ui_siege_leaders_sync = yes
	gg_plata_flags_sync = yes
	# EN/ES: playtest 2026-09-17 -- backstop: the Defensa's law of 1842 must not be lost if neither
	#     Arroyo Grande nor the dated dispatcher reached it / red de seguridad para la ley de 1842
	gg_abolition_dispatch = { DAYS = 2 }
	# EN/ES: the siege journal, opened by its event for both governments / el journal del sitio
	c:URU ?= { trigger_event = { id = gg_intro.4 days = 1 popup = yes } }
	c:CER ?= { trigger_event = { id = gg_intro.4 days = 1 popup = yes } }
	# EN: playtest 2026-09-19 -- everything with a historical date used to live here, in four tables
	#     of thirty lines (one per partition variant), counted in days from the split. The split is a
	#     date the player moves, so the whole chain slid with it: Caxias crossed in 1852. The dates
	#     are anchored to the calendar now, in events/guerra_grande_calendar.txt. What stays here is
	#     what is genuinely relative to the partition.
	# ES: todo lo que tiene fecha historica vivia aca, en cuatro tablas de treinta lineas (una por
	#     variante de particion), contadas en dias desde el corte. El corte es una fecha que el
	#     jugador mueve, asi que la cadena entera se corria con el: Caxias cruzaba en 1852. Ahora las
	#     fechas estan clavadas al calendario, en events/guerra_grande_calendar.txt. Aca queda lo que
	#     de verdad es relativo a la particion.
	# EN: Codex review 2026-09-19 -- the loans' watcher was scheduled on the Defensa alone, 200 days
	#     out, and only duplicated itself to both governments once it fired. A Cerrito that finished
	#     the Defensa inside those 200 days killed the pending event with the country, and the two
	#     reckoning events (gg_loan.5 and .6) became unreachable for that whole game. One copy on each
	#     government now, and the event's own guard keeps it from doubling.
	# ES: el vigia de los prestamos se agendaba solo sobre la Defensa, a 200 dias, y recien al salir
	#     se duplicaba a los dos gobiernos. Un Cerrito que terminara con la Defensa dentro de esos 200
	#     dias se llevaba puesto el evento pendiente, y los dos eventos del ajuste de cuentas quedaban
	#     inalcanzables en esa partida. Ahora va una copia en cada gobierno.
	c:URU ?= {
		trigger_event = { id = gg_loan.7 days = 200 }   # vigia del ajuste de cuentas (relativo)
	}
	c:CER ?= {
		trigger_event = { id = gg_loan.7 days = 200 }
	}
}

# EN: T7, C25/C26. Urquiza turns against Rosas: his weight moves to the Defensa, Rosas loses him,
#     the oribista chiefs go over, and the Cerrito answers (gg_powers.23).
# ES: T7, C25/C26. Urquiza se da vuelta: su peso pasa a la Defensa, Rosas lo pierde, los jefes
#     oribistas se pasan, y el Cerrito responde (gg_powers.23).
gg_urquiza_flip = {
	set_global_variable = gg_urquiza_flipped
	# EN/ES: T9, F4-3 -- Servando Gomez goes over; Garzon comes with Urquiza / Servando se pasa; Garzon viene con Urquiza
	c:CER ?= {
		random_scope_character = {
			limit = { has_template = URU_gg_servando_gomez }
			transfer_character = c:URU
		}
	}
	c:URU ?= {
		if = {
			limit = { NOT = { any_scope_character = { has_template = URU_gg_eugenio_garzon } } }
			create_character = {
				template = URU_gg_eugenio_garzon
				commander_rank = commander_rank_2
			}
		}
	}
	gg_patron_change = { PATRON = rosas DELTA = -20 }
	c:CER ?= {
		every_scope_war = {
			limit = { gg_is_the_war = yes }
			add_war_war_support = { target = c:CER value = -20 }
		}
		trigger_event = { id = gg_powers.23 days = 30 popup = yes }
	}
}

# EN: T8, C16 -- the postwar goes to the government left standing (the victor's rule of gg_peace.5).
# ES: T8, C16 -- la posguerra va al gobierno que quedo (la misma regla que gg_peace.5).
# -------------------------------------------------------------------------------------------------
# EN: WHAT BECOMES OF THE BEATEN PROVINCES WHEN THE GUERRA GRANDE ENDS. Rule asked for by Juan on
#     20/9/2026: «cuando una provincia se levanta y apago el levantamiento, la provincia queda como
#     vasalla para reflejar que no esta completamente controlada, y asi hasta la resolucion de la
#     Guerra Grande; si Rosas gana, por ejemplo, anexo las vasallas».
#     The first half of that was already true and by a different road: the war against a province is
#     fought with dp_gg_pacto_federal, whose war goal is make_protectorate, because Rosas never
#     annexed Corrientes or Tucuman -- he beat them and put a federal governor in. A protectorate is
#     a country whose foreign relations another conducts, which is the delegation itself, so a beaten
#     province counts as delegating again while it stays one. What was missing is the ending.
#     22/9/2026 (Juan): if the DEFENSA wins, nothing happens to them -- they stay protectorates of
#     Buenos Aires. A rule freeing them had been added without being asked for ("quien dijo eso") and
#     is gone.
#     WHY BY TAG AND NOT BY ITERATING SUBJECTS: annexing inside an every_subject_or_below changes the
#     list being walked. Fourteen tags are fourteen ifs (22/9/2026: the province countries joined).
# ES: QUE PASA CON LAS PROVINCIAS BATIDAS CUANDO TERMINA LA GUERRA GRANDE. Regla pedida por Juan el
#     20/9/2026: «cuando una provincia se levanta y apago el levantamiento, la provincia queda como
#     vasalla para reflejar que no esta completamente controlada, y asi hasta la resolucion de la
#     Guerra Grande; si Rosas gana, por ejemplo, anexo las vasallas».
#     La primera mitad ya era cierta y por otro camino: la guerra contra una provincia se pelea con
#     dp_gg_pacto_federal, cuyo war goal es make_protectorate, porque Rosas no anexo nunca a
#     Corrientes ni a Tucuman: las batio y les puso un gobernador federal. Un protectorado es un pais
#     cuyas relaciones exteriores lleva otro, que es la delegacion misma, asi que una provincia batida
#     cuenta como que vuelve a delegar mientras lo sea. Lo que faltaba era el desenlace.
#     22/9/2026 (Juan): si gana la DEFENSA no les pasa nada: siguen protectorado de Buenos Aires.
#     Se habia agregado una regla que las soltaba sin que nadie la pidiera, y se saco.
#     POR QUE POR TAG Y NO ITERANDO SUJETOS: anexar adentro de un every_subject_or_below cambia la
#     lista que se esta recorriendo. Tres tags son tres ifs.
# -------------------------------------------------------------------------------------------------
gg_argentina_vassals_settle = {
	# EN: 22/9/2026 (Juan) -- the beaten provinces are annexed when the Federal Pact is settled, whatever
	#     the settlement ("se anexan cuando se salde el pacto federal"), not when the Guerra Grande ends.
	#     Called from gg_pact_resolve. Only those still subject to Argentina.
	# ES: las provincias batidas se anexan cuando se salda el Pacto Federal, sea cual sea el cierre, y no
	#     al terminar la Guerra Grande. Lo llama gg_pact_resolve. Solo las que siguen sujetas.
	# EN/ES: 22/9/2026 -- every province country too (TUC ... SNL), one if per tag for the same reason
	#     / y todo pais provincial, un if por tag por la misma razon
	c:ARG ?= {
		if = {
			limit = {
				any_subject_or_below = {
					OR = {
						c:CLN ?= this
						gg_is_province_country = yes
					}
					is_subject_of = c:ARG
				}
			}
			if = { limit = { c:CRR ?= { is_subject_of = c:ARG } } annex = c:CRR }
			if = { limit = { c:CLN ?= { is_subject_of = c:ARG } } annex = c:CLN }
			if = { limit = { c:ENR ?= { is_subject_of = c:ARG } } annex = c:ENR }
			if = { limit = { c:TUC ?= { is_subject_of = c:ARG } } annex = c:TUC }
			if = { limit = { c:SLT ?= { is_subject_of = c:ARG } } annex = c:SLT }
			if = { limit = { c:JUJ ?= { is_subject_of = c:ARG } } annex = c:JUJ }
			if = { limit = { c:CTM ?= { is_subject_of = c:ARG } } annex = c:CTM }
			if = { limit = { c:LRJ ?= { is_subject_of = c:ARG } } annex = c:LRJ }
			if = { limit = { c:CBA ?= { is_subject_of = c:ARG } } annex = c:CBA }
			if = { limit = { c:SFE ?= { is_subject_of = c:ARG } } annex = c:SFE }
			if = { limit = { c:SDE ?= { is_subject_of = c:ARG } } annex = c:SDE }
			if = { limit = { c:MZA ?= { is_subject_of = c:ARG } } annex = c:MZA }
			if = { limit = { c:SJN ?= { is_subject_of = c:ARG } } annex = c:SJN }
			if = { limit = { c:SNL ?= { is_subject_of = c:ARG } } annex = c:SNL }
			trigger_event = { id = gg_argentina.80 days = 2 popup = yes }
		}
	}
}

gg_postwar_dispatch = {
	# EN: Codex review 2026-09-19 -- this used to send the Cerrito to gg_peace.8 whenever the blancos
	#     were ahead on the score, even with the Defensa still alive. gg_peace.8 only changes the tag
	#     when URU is gone, so in those games nobody changed tag AND the postwar went to the Defensa:
	#     the menu on one side and the postwar on the other. The Cerrito takes the Republic's name
	#     only when it really absorbed it; otherwise the postwar goes to whoever is left, and if both
	#     are left, to the side that won.
	# ES: mandaba al Cerrito a gg_peace.8 con que los blancos fueran arriba en el puntaje, aun con la
	#     Defensa viva. gg_peace.8 solo cambia el tag si URU ya no existe, asi que en esas partidas no
	#     cambiaba de tag nadie Y la posguerra se iba a la Defensa: el menu de un lado y la posguerra
	#     del otro. El Cerrito recupera el nombre de la Republica solo si de verdad la absorbio; si
	#     no, la posguerra es de quien haya quedado, y si quedaron los dos, del que gano.
	if = {
		limit = {
			exists = c:CER
			NOT = { exists = c:URU }
		}
		# EN/ES: the Cerrito takes the Republic's tag first (gg_peace.8) / el Cerrito recupera el tag
		c:CER = { trigger_event = { id = gg_peace.8 days = 1 } }
	}
	else_if = {
		limit = { NOT = { exists = c:CER } }
		c:URU ?= { trigger_event = { id = gg_postwar.1 days = 60 } }
	}
	else_if = {
		limit = { gg_cerrito_won_militarily = yes }
		c:CER ?= { trigger_event = { id = gg_postwar.1 days = 60 } }
	}
	else = {
		c:URU ?= { trigger_event = { id = gg_postwar.1 days = 60 } }
	}
}

# EN: T9 -- the four characters of 1843, each on his side. Guarded so a second call does nothing.
# ES: T9 -- los cuatro personajes de 1843, cada uno en su bando. Con guarda para no duplicar.
gg_spawn_war_characters = {
	if = {
		limit = { NOT = { has_global_variable = gg_war_characters_spawned } }
		set_global_variable = gg_war_characters_spawned
		c:URU ?= {
			create_character = { template = URU_gg_melchor_pacheco_y_obes }
		}
		c:CER ?= {
			create_character = { template = URU_gg_carlos_villademoros }
			create_character = { template = URU_gg_juan_francisco_giro }
			create_character = {
				template = URU_gg_servando_gomez
				commander_rank = commander_rank_1
			}
		}
	}
}

# EN: THE PRESIDENCY, RAILROADED (Juan, 2026-09-17). The Republic's head of state follows history,
#     not the ballot: an election in the middle of the war used to hand the presidency to whatever
#     party politician won the vote, in either path. Sources (Wikipedia ES): Oribe president from
#     1835 until he resigned on 24 October 1838; Gabriel Antonio Pereira holds the executive from
#     1 November 1838, elected by the Senate because the president of the Senate, Carlos Anaya, had
#     sailed for Buenos Aires; Rivera takes the presidency on 1 March 1839 and the Congress declares
#     his term over on 1 March 1843, replacing him with Joaquin Suarez at the head of the Gobierno de
#     la Defensa; the Cerrito is Oribe's, who claimed the mandate the revolution of 1838 cut short;
#     Suarez hands over to Bernardo Berro on 15 February 1852, and Giro takes office on 1 March.
# ES: LA PRESIDENCIA, POR CARRIL FIJO. La jefatura de Estado sigue la historia y no la urna: una
#     eleccion en mitad de la guerra le daba la presidencia al politico de partido que ganara la
#     votacion, en los dos caminos. Fuentes (Wikipedia ES) en el parrafo de arriba.
gg_ruler_set = {
	if = {
		limit = { any_scope_character = { has_template = $TEMPLATE$ } }
		random_scope_character = {
			limit = { has_template = $TEMPLATE$ }
			set_character_as_ruler = yes
		}
	}
	# EN: AND IF HE IS ALIVE SOMEWHERE ELSE, HE IS FETCHED, NOT COPIED. Added 20/9/2026. This used to
	#     go straight from «not here» to create_character, and the case that breaks is the one the
	#     whole mod turns on: Oribe leaves Montevideo in October 1838 for Buenos Aires, and four years
	#     later the Cerrito is founded on his army. The Cerrito did not have him -- he was in
	#     Argentina -- so it made a second Manuel Oribe, and the game had two of the same man, one
	#     besieging Montevideo and one in Rosas' government.
	#     A ruler who exists is moved. Only a ruler who exists nowhere is created.
	# ES: Y SI ESTA VIVO EN OTRA PARTE, SE LO VA A BUSCAR, NO SE LO COPIA. Agregado el 20/9/2026. Esto
	#     saltaba de «no esta aca» directo a create_character, y el caso que lo rompe es aquel sobre el
	#     que gira el mod entero: Oribe se va de Montevideo en octubre de 1838 a Buenos Aires, y cuatro
	#     anios despues el Cerrito se funda sobre su ejercito. El Cerrito no lo tenia -- estaba en la
	#     Argentina -- asi que fabricaba un segundo Manuel Oribe, y el juego tenia dos del mismo
	#     hombre, uno sitiando Montevideo y otro en el gobierno de Rosas.
	#     Un gobernante que existe se muda. Solo se crea el que no existe en ninguna parte.
	else_if = {
		limit = {
			any_country = {
				any_scope_character = { has_template = $TEMPLATE$ }
			}
		}
		save_scope_as = gg_ruler_claimant
		random_country = {
			limit = {
				any_scope_character = { has_template = $TEMPLATE$ }
			}
			random_scope_character = {
				limit = { has_template = $TEMPLATE$ }
				transfer_character = scope:gg_ruler_claimant
			}
		}
		random_scope_character = {
			limit = { has_template = $TEMPLATE$ }
			set_character_as_ruler = yes
		}
	}
	# EN/ES: 22/9/2026 (Codex) -- a man in exile is brought home, not duplicated: the same
	#     transfer_character vanilla uses on the exile pool (garibaldi_events.txt) / al exiliado se lo trae
	else_if = {
		limit = { any_character_in_exile_pool = { has_template = $TEMPLATE$ } }
		save_scope_as = gg_ruler_claimant
		random_character_in_exile_pool = {
			limit = { has_template = $TEMPLATE$ }
			transfer_character = scope:gg_ruler_claimant
		}
		random_scope_character = {
			limit = { has_template = $TEMPLATE$ }
			set_character_as_ruler = yes
		}
	}
	else = {
		create_character = {
			template = $TEMPLATE$
			ruler = yes
		}
	}
}

# EN/ES: who should be sitting in the chair right now, in this country / quien deberia estar en el
#     sillon ahora mismo, en este pais
gg_president_historical = {
	if = {
		limit = { has_variable = guerra_grande_cerrito_government }
		gg_ruler_set = { TEMPLATE = URU_Manuel_Oribe }
	}
	else_if = {
		limit = {
			has_variable = guerra_grande_defensa_government
			OR = {
				NOT = { has_global_variable = gg_peace_settled }
				has_global_variable = gg_president_railroad
			}
		}
		gg_ruler_set = { TEMPLATE = URU_joaquin_suarez }
	}
	else_if = {
		limit = {
			has_variable = guerra_grande_blanco_path
			NOT = { has_global_variable = gg_rivera_presidency }
		}
		gg_ruler_set = { TEMPLATE = URU_Manuel_Oribe }
	}
	else_if = {
		limit = {
			has_global_variable = gg_rivera_presidency
			year >= 1839
		}
		gg_ruler_set = { TEMPLATE = URU_Fructuoso_Rivera }
	}
	else_if = {
		limit = { has_global_variable = gg_rivera_presidency }
		gg_ruler_set = { TEMPLATE = URU_gg_gabriel_antonio_pereira }
	}
}

# EN: fighting on after the arbiter declared this government beaten (playtest 2026-09-17): the
#     exhaustion counter restarts, the arbiter is shut for a year, and the decision costs what such
#     a decision costs.
# ES: seguir peleando despues de que el arbitro declaro agotado a este gobierno: el contador vuelve
#     a cero, el arbitro se cierra por un anio y la decision cuesta lo que cuesta.
gg_peace_fight_on = {
	remove_global_variable = gg_peace_offered
	set_global_variable = { name = gg_peace_cooldown days = 365 }
	if = {
		limit = { has_variable = guerra_grande_cerrito_government }
		set_global_variable = { name = gg_cerrito_exhausted value = 0 }
	}
	else = {
		set_global_variable = { name = gg_defensa_exhausted value = 0 }
	}
	every_scope_war = {
		limit = { gg_is_the_war = yes }
		add_war_war_support = { target = root value = -10 }
	}
	add_radicals = { value = small_radicals }
}


# EN/ES: a named province (or bloc) stops delegating, once and once only. COUNT negativo.
# -------------------------------------------------------------------------------------------------
# EN: A CLAIM OPENED, AND A CLAIM ANSWERED. Written 20/9/2026, and the reason is Juan's, playing:
#     «los eventos de la previa son todos para corrientes! y no estan conectadas a la mecanica del
#     journal no entiendo que estas haciendo».
#     He was right twice. Of the seven build-up events, four were Corrientes' -- and not one of the
#     seven touched the Federal Pact at all. So the player spent three years answering petitions,
#     refusing demands and sending generals, and the panel that is supposed to be the state of the
#     Confederation did not move once until a province seceded in 1839. Decisions that leave no trace
#     on the only thing that shows the state of the thing they are about are not decisions.
#     A province now has three states, not two: it delegates, it delegates WITH A CLAIM OPEN against
#     Buenos Aires, or it has withdrawn the charge. Every build-up event opens or answers a claim, so
#     the panel is alive from 1836 and the player can watch the grievances pile up before anything
#     breaks. Withdrawing the charge closes the claim, because a province that has left has stopped
#     asking.
# ES: UN RECLAMO ABIERTO, Y UN RECLAMO CONTESTADO. Escrito el 20/9/2026, y la razon es de Juan,
#     jugando: «los eventos de la previa son todos para corrientes! y no estan conectadas a la
#     mecanica del journal no entiendo que estas haciendo».
#     Tenia razon dos veces. De los siete eventos de la previa, cuatro eran de Corrientes -- y
#     ninguno de los siete tocaba el Pacto Federal. Asi que el jugador se pasaba tres anios
#     contestando pliegos, negando reclamos y mandando generales, y el panel que representa el estado
#     de la Confederacion no se movia una sola vez hasta que una provincia se separaba en 1839. Las
#     decisiones que no dejan rastro en lo unico que muestra el estado de aquello de lo que tratan no
#     son decisiones.
#     Una provincia tiene ahora tres estados y no dos: delega, delega CON UN RECLAMO ABIERTO contra
#     Buenos Aires, o retiro el encargo. Cada evento de la previa abre o contesta un reclamo, asi que
#     el panel esta vivo desde 1836 y el jugador ve acumularse los agravios antes de que se rompa
#     nada. Retirar el encargo cierra el reclamo, porque una provincia que se fue dejo de pedir.
# -------------------------------------------------------------------------------------------------
gg_reclamo_abre = {
	if = {
		limit = {
			NOT = { has_global_variable = gg_reclamo_$KEY$ }
			# EN: A late answer to a five-year popup cannot reopen a claim for a province that has
			#     already withdrawn its delegation. ES: Una respuesta tardia a un popup de cinco
			#     anios no puede reabrir el reclamo de una provincia que ya retiro su delegacion.
			NOT = { has_global_variable = gg_delegation_gone_$KEY$ }
		}
		set_global_variable = gg_reclamo_$KEY$
		# EN/ES: no hay contador aparte: la lista del panel muestra CUALES, que es mejor que cuantos
		#     / no separate counter: the panel's list shows WHICH, which beats how many
	}
}

gg_reclamo_cierra = {
	if = {
		limit = { has_global_variable = gg_reclamo_$KEY$ }
		remove_global_variable = gg_reclamo_$KEY$
	}
}

gg_delegation_revoked = {
	if = {
		limit = { NOT = { has_global_variable = gg_delegation_gone_$KEY$ } }
		set_global_variable = gg_delegation_gone_$KEY$
		# EN/ES: y el reclamo se cierra: la que se fue dejo de pedir / a province that left stopped asking
		gg_reclamo_cierra = { KEY = $KEY$ }
		change_global_variable = {
			name = gg_delegating_provinces
			add = $COUNT$
		}
	}
	# EN: AND THE COUNT THAT NEVER GOES BACK DOWN (Codex, 20/9/2026). The count of delegating
	#     provinces is a photograph of today: a Rosas who fought the interior for four years and took
	#     it back looks like one nobody defied. gg_provincias_alzadas only ever goes up, guarded by its
	#     own mark, so a province is counted once however often it is lost and retaken; the balance
	#     of the interior (gg_argentina.20) reads it.
	# ES: Y LA CUENTA QUE NO VUELVE A BAJAR (Codex, 20/9/2026). La cuenta de provincias que delegan
	#     es una foto de hoy: un Rosas que peleo cuatro anios el interior y lo recupero se ve igual que
	#     uno al que nadie desafio. gg_provincias_alzadas solo sube, guardada por su propia marca, asi
	#     que una provincia se cuenta una vez por mas que se pierda y se recupere; la lee el balance del
	#     interior (gg_argentina.20).
	if = {
		limit = { NOT = { has_global_variable = gg_alzada_$KEY$ } }
		set_global_variable = gg_alzada_$KEY$
		change_global_variable = {
			name = gg_provincias_alzadas
			add = 1
		}
	}
}

# EN/ES: y vuelve a delegar, recuperada por las armas. COUNT positivo.
gg_delegation_restored = {
	if = {
		limit = { has_global_variable = gg_delegation_gone_$KEY$ }
		remove_global_variable = gg_delegation_gone_$KEY$
		change_global_variable = {
			name = gg_delegating_provinces
			add = $COUNT$
		}
	}
}
