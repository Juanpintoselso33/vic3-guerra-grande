namespace = gg_argentina_cal

# -------------------------------------------------------------------------------------------------
# EN: THE DATED CALENDAR OF THE ARGENTINE SIDE. Twenty-five relays carry the preludes, the births
#     and the interior campaign to their delivery dates, counted in days from 1 January 1836.
#
#     WHY RELAYS AND NOT THE EVENTS THEMSELVES. A scheduled event's trigger is read WHEN IT IS
#     SCHEDULED, not on its future date. Scheduling the birth of Corrientes directly on day 1159
#     would test, on 1 January 1836, whether Corrientes does not yet exist and Argentina still holds
#     its states -- and would then fire on 6 March 1839 no matter what happened in between. So the
#     relay is unconditional, and every condition lives inside it, read on the day it matters. This
#     is the rule that cost the mod the partition calendar, Oribe's 1839 exit and three dated
#     events of the powers.
#
#     WHY ONLY ONE COPY. The Uruguayan calendar schedules two copies of every relay because the
#     Republic changes tag at the partition and can lose what is queued on it. These four are
#     scheduled on ARGENTINA, which never changes tag in this mod, so one copy is right and a second
#     would only be a second thing to keep in step.
#
#     DELIVERY MARKS. Single-recipient events write gg_arg_cal_done_N in their own immediate.
#     Relays 1, 2 and 4 still mark the attempt: each has two alternative recipients, so moving
#     their mark requires resolving the single-writer constraint. These relays have no retry.
#
#     THE YEARS ARE 365 DAYS LONG, with no leap days -- the engine's years, not the calendar's. The
#     offsets were computed by hand and checked one by one.
# ES: EL CALENDARIO FECHADO DEL LADO ARGENTINO. Veinticinco relevos llevan las previas, los
#     nacimientos y la campania del interior a sus fechas de entrega, contadas desde el 1 de enero
#     de 1836.
#
#     POR QUE RELEVOS Y NO LOS EVENTOS DIRECTAMENTE. El trigger de un evento agendado se lee CUANDO
#     SE AGENDA, no en su fecha futura. Agendar el nacimiento de Corrientes directo al dia 1159
#     preguntaria, el 1 de enero de 1836, si Corrientes todavia no existe y si la Argentina conserva
#     sus estados -- y despues saldria el 6 de marzo de 1839 pasara lo que pasara en el medio. Por
#     eso el relevo va sin condiciones y toda condicion vive adentro, leida el dia que importa. Es la
#     regla que le costo al mod el calendario de la particion, la salida de Oribe en 1839 y tres
#     eventos fechados de las potencias.
#
#     POR QUE UNA SOLA COPIA. El calendario oriental agenda dos copias de cada relevo porque la
#     Republica cambia de tag en la particion y puede perder lo que tenga encolado. Estos cuatro se
#     agendan sobre la ARGENTINA, que en este mod no cambia de tag nunca, asi que una copia es lo
#     correcto y una segunda seria solo otra cosa que mantener sincronizada.
#
#     MARCAS DE ENTREGA. Los eventos de destinatario unico escriben gg_arg_cal_done_N en su immediate.
#     Los relevos 1, 2 y 4 aun marcan el intento: tienen dos destinatarios alternativos y mover
#     su marca exige resolver la restriccion de escritor unico. Estos relevos no tienen reintento.
#
#     LOS ANIOS DEL MOTOR SON DE 365 DIAS, sin bisiestos. Los corrimientos se calcularon a
#     mano y se verificaron uno por uno.
# -------------------------------------------------------------------------------------------------

# -------------------------------------------------------------------------------------------------
# EN: «LA ARGENTINA» IS REQUIRED FOR THIS SIDE, and every relay checks it. Decision of 19 September
#     2026, after Codex's review caught it: of the eight Argentine states this side needs, only
#     STATE_CORRIENTES, STATE_TUCUMAN and STATE_JUJUY exist in vanilla (the count said two until
#     20 September 2026, when a second review checked it against the map files: Jujuy is there). Entre Rios, Cordoba, Salta, Misiones, La
#     Rioja and Catamarca are provinces that «la Argentina» adds -- vanilla does not split Argentina
#     into its historical provinces at all. The whole Argentine side was built reading the COMPATCH's
#     map, which has them, without checking that the standalone mod does not.
#     So rather than pretend, each relay asks for a state that only exists with «la Argentina»
#     loaded. Without it, the four relays do nothing, nothing is created, nothing breaks, and the
#     Uruguayan side plays exactly as it always did. The requirement is stated in README.md.
# ES: «LA ARGENTINA» ES REQUISITO DE ESTE LADO, y cada relevo lo chequea. Decision del 19/9/2026,
#     despues de que la revision de Codex lo agarrara: de los ocho estados argentinos que este lado
#     necesita, en vanilla existen solo STATE_CORRIENTES, STATE_TUCUMAN y STATE_JUJUY (la cuenta
#     decia dos hasta el 20/9/2026, cuando una segunda revision la chequeo contra los archivos del
#     mapa: Jujuy esta). Entre Rios, Cordoba, Salta,
#     Misiones, La Rioja y Catamarca son provincias que agrega «la Argentina» -- vanilla no parte a la
#     Argentina en sus provincias historicas. Todo este lado se armo leyendo el mapa del COMPATCH, que
#     si las tiene, sin chequear que el mod solo no.
#     Asi que en vez de disimular, cada relevo pide un estado que solo existe con «la Argentina»
#     cargada. Sin ella, los cuatro relevos no hacen nada, no se crea nada, no se rompe nada, y el
#     lado oriental se juega exactamente como siempre. El requisito esta dicho en README.md.
# -------------------------------------------------------------------------------------------------

# EN/ES: 6/3/1839 (dia 1159) -- Corrientes se separa y le declara la guerra a Rosas
gg_argentina_cal.1 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.1|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_1 }
					# EN/ES: guarda de «la Argentina»: Entre Rios no existe sin ella / the guard
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					NOT = { exists = c:CRR }
					exists = s:STATE_CORRIENTES.region_state:ARG
				}
				set_global_variable = gg_arg_cal_done_1
				if = {
					# EN: One refusal is enough to leave the old dispute alive. Only two concessions,
					#     and therefore an agravio count of zero, avert the secession.
					# ES: Una negativa alcanza para dejar vivo el pleito. Solo dos concesiones, y por
					#     eso una cuenta de agravio en cero, evitan la secesion.
					limit = {
						has_global_variable = gg_corrientes_agravio
						global_var:gg_corrientes_agravio = 0
					}
					c:ARG = { trigger_event = { id = gg_argentina.64 days = 0 popup = yes } }
				}
				else = {
					c:ARG = { trigger_event = { id = gg_argentina.1 days = 0 popup = yes } }
				}
			}
		}
	}
}

# EN/ES: 7/4/1840 (dia 1556) -- Tucuman se pronuncia y nace la Coalicion del Norte
gg_argentina_cal.2 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.2|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_2 }
					# EN/ES: guarda de «la Argentina»: Entre Rios no existe sin ella / the guard
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					NOT = { exists = c:CLN }
					# EN/ES: Tucuman es la capital de la Coalicion: sin ella no hay pronunciamiento
					#     / Tucuman is the Coalition's capital: without it there is no pronouncement
					exists = s:STATE_TUCUMAN.region_state:ARG
				}
				set_global_variable = gg_arg_cal_done_2
				if = {
					# EN: Only three concessions erase the three pending northern claims.
					# ES: Solo tres concesiones borran los tres reclamos pendientes del norte.
					limit = {
						has_global_variable = gg_norte_agravio
						global_var:gg_norte_agravio = 0
					}
					c:ARG = { trigger_event = { id = gg_argentina.70 days = 0 popup = yes } }
				}
				else = {
					c:ARG = { trigger_event = { id = gg_argentina.2 days = 0 popup = yes } }
				}
			}
		}
	}
}

# EN/ES: 10/10/1840 (dia 1742) -- Cordoba adhiere a una Coalicion que ya existe
gg_argentina_cal.3 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.3|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_3 }
					# EN/ES: guarda de «la Argentina»: Entre Rios no existe sin ella / the guard
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					# EN/ES: no crea pais: le suma un estado a uno que ya existe. Si la Coalicion no
					#     llego a nacer, o ya cayo, no hay a quien adherir.
					#     / it creates no country: it adds a state to one that exists. If the
					#     Coalition never was born, or has already fallen, there is nobody to join.
					exists = c:CLN
					exists = s:STATE_CORDOBA.region_state:ARG
				}
				c:ARG = { trigger_event = { id = gg_argentina.4 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 1/5/1851 (dia 5595) -- el Pronunciamiento de Urquiza
gg_argentina_cal.4 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.4|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_4 }
					# EN/ES: guarda de «la Argentina»: Entre Rios no existe sin ella / the guard
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					# EN/ES: 23/9 (Juan, §2) -- or the loyal Entre Rios of Alcaraz / o el Entre Rios leal de Alcaraz
					gg_entre_rios_with_rosas = yes
				}
				set_global_variable = gg_arg_cal_done_4
				# EN: 23/9/2026 (Juan) -- Urquiza weighs it (gg_urquiza_discontent), instead of an all-or-nothing
				#     checklist: 4+ pronounces, 2-3 a formal representation, 1 or less the negotiated confederation.
				# ES: Urquiza lo pesa, en vez de una lista de todo o nada.
				set_global_variable = { name = gg_urquiza_discontent_now value = gg_urquiza_discontent }
				# EN/ES: 23/9 -- the Pact's game rule decides before the weighing / la regla del Pacto decide antes de pesar
				if = {
					limit = { gg_pact_negotiated = yes }
					c:ARG = { trigger_event = { id = gg_argentina.71 days = 0 popup = yes } }
				}
				else_if = {
					limit = { gg_pact_forced_war = yes }
					c:ARG = { trigger_event = { id = gg_argentina.3 days = 0 popup = yes } }
				}
				else_if = {
					limit = { gg_urquiza_discontent <= 1 }
					c:ARG = { trigger_event = { id = gg_argentina.71 days = 0 popup = yes } }
				}
				else_if = {
					limit = { gg_urquiza_discontent <= 3 }
					c:ARG = { trigger_event = { id = gg_urquiza.1 days = 0 popup = yes } }
				}
				else = {
					c:ARG = { trigger_event = { id = gg_argentina.3 days = 0 popup = yes } }
				}
			}
		}
	}
}

# EN/ES: 31/3/1839 (dia 1184) -- Pago Largo: muere Beron de Astrada
gg_argentina_cal.5 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.5|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_5 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
				}
				c:ARG = { trigger_event = { id = gg_argentina.10 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 28/11/1840 (dia 1791) -- Quebracho Herrado
gg_argentina_cal.6 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.6|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_6 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
				}
				c:ARG = { trigger_event = { id = gg_argentina.11 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 19/9/1841 (dia 2086) -- Famailla, y despues Metan
gg_argentina_cal.7 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.7|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_7 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
				}
				c:ARG = { trigger_event = { id = gg_argentina.12 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 16/2/1843 (dia 2601) -- el balance del interior, el dia que Oribe cruza
gg_argentina_cal.8 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.8|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_8 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
				}
				c:ARG = { trigger_event = { id = gg_argentina.20 days = 0  } }
			}
		}
	}
}

# EN/ES: 1/2/1852 (dia 5871) -- renuncia Pacheco
gg_argentina_cal.9 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.9|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_9 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					# EN: only if there is a war to lose. The comment said this and the code did not:
					#     it asked whether Entre Rios EXISTED, which is a different question. A 1851
					#     war that ended in a status quo left Entre Rios standing and at peace, and
					#     on 1 February 1852 Pacheco still resigned and on the third «the hour of
					#     Caseros» still arrived, announcing an Ejercito Grande a few leagues off
					#     that was not marching anywhere. Corrected 20/9/2026 (Codex).
					# ES: solo si hay una guerra que perder. El comentario lo decia y el codigo no: lo
					#     que preguntaba era si Entre Rios EXISTIA, que es otra pregunta. Una guerra
					#     de 1851 terminada en statu quo dejaba a Entre Rios en pie y en paz, y el 1
					#     de febrero de 1852 Pacheco renunciaba igual y el 3 llegaba igual «la hora de
					#     Caseros», anunciando un Ejercito Grande a unas leguas que no marchaba a
					#     ningun lado. Corregido el 20/9/2026 (Codex).
					exists = c:ENR
					c:ARG = { has_war_with = c:ENR }
					has_global_variable = gg_ejercito_grande_created
				}
				c:ARG = { trigger_event = { id = gg_argentina.40 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 3/2/1852 (dia 5873) -- la hora de Caseros
gg_argentina_cal.10 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.10|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_10 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					# EN/ES: la misma guarda que el relevo de Pacheco / the same guard as Pacheco's relay
					exists = c:ENR
					c:ARG = { has_war_with = c:ENR }
					has_global_variable = gg_ejercito_grande_created
				}
				c:ARG = { trigger_event = { id = gg_argentina.41 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 24/9/1840 (dia 1726) -- el pacto de la Liga y Brizuela Director
gg_argentina_cal.11 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.11|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_11 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					# EN/ES: sin Liga no hay Director / no League, no Director
					exists = c:CLN
				}
				c:ARG = { trigger_event = { id = gg_argentina.5 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 28/11/1841 (dia 2156) -- Caa Guazu: Paz deshace a Echague
gg_argentina_cal.12 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.12|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_12 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					# EN/ES: o la provincia esta en pie, o Rosas la tiene y se la pueden quitar. Si no
					#     es ninguna de las dos, Caa Guazu no tiene donde pasar / either the province
					#     stands, or Rosas holds it and it can be taken from him
					OR = {
						exists = c:CRR
						exists = s:STATE_CORRIENTES.region_state:ARG
					}
				}
				c:ARG = { trigger_event = { id = gg_argentina.6 days = 0 popup = yes } }
			}
		}
	}
}

# EN/ES: 1/4/1843 (dia 2645) -- el Paso de los Libres: vuelven los Madariaga
gg_argentina_cal.13 = {
	type = country_event
	hidden = yes

	immediate = {
		debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.13|" # GG_TRAZA
		hidden_effect = {
			if = {
				limit = {
					NOT = { has_global_variable = gg_arg_cal_done_13 }
					exists = s:STATE_ENTRE_RIOS
					exists = c:ARG
					OR = {
						exists = c:CRR
						exists = s:STATE_CORRIENTES.region_state:ARG
					}
				}
				c:ARG = { trigger_event = { id = gg_argentina.7 days = 0 popup = yes } }
			}
		}
	}
}

# -------------------------------------------------------------------------------------------------
# EN: PREHISTORY RELAYS. Their scheduling dates are documented below. The two April dates are design
#     dates for petitions in continuing disputes, not claimed historical anniversaries; the source
#     establishes the programme and the years. The 1838 and birth dates are historical dates.
# ES: RELEVOS DE LA PREVIA. Sus fechas de agenda quedan documentadas abajo. Las dos fechas de abril
#     son fechas de diseno para pliegos de pleitos permanentes, no aniversarios historicos; la fuente
#     documenta el programa y los anios. Las fechas de 1838 y de los nacimientos son historicas.
# SOURCE / FUENTE: docs/guerra-grande-argentina/historia-argentina.md
# -------------------------------------------------------------------------------------------------

# EN/ES: 1/4/1836 (day/dia 90) -- ports and free navigation / puertos y libre navegacion
gg_argentina_cal.14 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.14|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_14 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = s:STATE_CORRIENTES.region_state:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.61 days = 0 popup = yes } }
		}
	} }
}

# EN/ES: 1/4/1837 (day/dia 455) -- customs sharing / coparticipacion aduanera
gg_argentina_cal.15 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.15|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_15 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = s:STATE_CORRIENTES.region_state:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.62 days = 0 popup = yes } }
		}
	} }
}

# EN/ES: 28/3/1838 (day/dia 816) -- the French blockade begins / empieza el bloqueo frances
gg_argentina_cal.16 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.16|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_16 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = s:STATE_CORRIENTES.region_state:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.63 days = 0 popup = yes } }
		}
	} }
}

# EN: 1/12/1838 (day 1064) -- delivery date for the news of Heredia's murder; the source gives 1838,
#     not a day. ES: fecha de entrega de la noticia del asesinato; la fuente da 1838, no un dia.
gg_argentina_cal.17 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.17|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_17 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.65 days = 0 popup = yes } }
		}
	} }
}

# EN: 1/3/1840 (day 1519) -- scheduling date within the documented month. The arms petition opens
#     first and delivers the existing Lamadrid decision when answered.
# ES: fecha de agenda dentro del mes documentado. Primero se abre el pliego de las armas y, al
#     contestarlo, entrega la decision existente sobre Lamadrid.
gg_argentina_cal.18 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.18|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_18 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = s:STATE_TUCUMAN.region_state:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.68 days = 0 popup = yes } }
		}
	} }
}

# EN: 1/12/1850 (day 5444) -- delivery date within the documented year. ES: fecha de entrega dentro
#     del anio documentado.
gg_argentina_cal.19 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.19|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_19 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = s:STATE_ENTRE_RIOS.region_state:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.67 days = 0 popup = yes } }
		}
	} }
}

# EN: August 1846 -- the source gives the month, not the day. Day 3862 is a technical first-of-month
#     delivery anchor and the event text claims no greater precision.
# ES: agosto de 1846 -- la fuente da el mes, no el dia. El dia 3862 es un ancla tecnica de entrega
#     al comienzo del mes y el texto del evento no pretende mayor precision.
# SOURCE / FUENTE: docs/guerra-grande-argentina/historia-argentina.md, «1843-1847 — Corrientes sola,
# y el Paraguay» [D].
gg_argentina_cal.20 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.20|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_20 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = c:CRR
				c:ARG = { has_war_with = c:CRR }
				exists = s:STATE_ENTRE_RIOS.region_state:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.69 days = 0 popup = yes } }
		}
	} }
}

# -------------------------------------------------------------------------------------------------
# EN: THE WAR OF THE PARANA. The blockade is deliberately delivered on the mod's existing date,
#     18 September 1845. The other three dates are the documented days. As above, all conditions
#     live in the relays because scheduled-event triggers are read when the event is queued.
# ES: LA GUERRA DEL PARANA. El bloqueo se entrega a proposito en la fecha que ya usa el mod, 18 de
#     septiembre de 1845. Las otras tres son las fechas documentadas. Como arriba, todas las
#     condiciones viven en los relevos porque el trigger se lee cuando el evento se agenda.
# SOURCE / FUENTE: docs/guerra-grande-argentina/historia-argentina.md, cronología del bloqueo [D].
# -------------------------------------------------------------------------------------------------

# EN/ES: 18/9/1845 (day/dia 3545) -- the blockade from Buenos Aires / el bloqueo desde Buenos Aires
gg_argentina_cal.21 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.21|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_21 }
				# EN/ES: C10 -- a link of the intervention chain / un eslabon de la cadena de la intervencion
				gg_plata_intervention_at = { MONTHS = 2 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				has_global_variable = guerra_grande_open_war
			}
			c:ARG = { trigger_event = { id = gg_argentina.22 days = 0 popup = yes } }
		}
		# EN: C10 -- the intervention has no fixed date any more, so this relay waits for it instead of
		#     firing once on the historical day and being lost. / la intervencion ya no tiene fecha fija:
		#     este relevo la espera en vez de salir una vez el dia historico y perderse.
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_21 }
				year < 1856
				exists = s:STATE_ENTRE_RIOS
				NOT = { has_global_variable = gg_peace_settled }
			}
			trigger_event = { id = gg_argentina_cal.21 days = 30 }
		}
	} }
}

# EN/ES: 20/11/1845 (day/dia 3608) -- Obligado, only after fortifying / solo si se fortifico
gg_argentina_cal.22 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.22|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_22 }
				# EN/ES: C10 -- a link of the intervention chain / un eslabon de la cadena de la intervencion
				gg_plata_intervention_at = { MONTHS = 4 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				has_global_variable = gg_parana_resistance
			}
			c:ARG = { trigger_event = { id = gg_argentina.23 days = 0 popup = yes } }
		}
		# EN: C10 -- the intervention has no fixed date any more, so this relay waits for it instead of
		#     firing once on the historical day and being lost. / la intervencion ya no tiene fecha fija:
		#     este relevo la espera en vez de salir una vez el dia historico y perderse.
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_22 }
				year < 1856
				exists = s:STATE_ENTRE_RIOS
				NOT = { has_global_variable = gg_peace_settled }
				# EN/ES: CB-04 (23/9/2026) -- Buenos Aires answered the blockade without fortifying the Parana:
				#     there is no Obligado to wait for (it used to retry a hundred times) / se contesto el
				#     bloqueo sin fortificar el Parana: no hay Obligado que esperar
				NAND = {
					has_global_variable = gg_parana_answered
					NOT = { has_global_variable = gg_parana_resistance }
				}
			}
			trigger_event = { id = gg_argentina_cal.22 days = 30 }
		}
	} }
}

# EN/ES: 4/6/1846 (day/dia 3805) -- Quebracho, only if resistance continued / solo si siguio
gg_argentina_cal.23 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.23|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_23 }
				# EN/ES: C10 -- a link of the intervention chain / un eslabon de la cadena de la intervencion
				gg_plata_intervention_at = { MONTHS = 10 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				has_global_variable = gg_parana_resistance_continues
			}
			c:ARG = { trigger_event = { id = gg_argentina.24 days = 0 popup = yes } }
		}
		# EN: C10 -- the intervention has no fixed date any more, so this relay waits for it instead of
		#     firing once on the historical day and being lost. / la intervencion ya no tiene fecha fija:
		#     este relevo la espera en vez de salir una vez el dia historico y perderse.
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_23 }
				year < 1856
				exists = s:STATE_ENTRE_RIOS
				NOT = { has_global_variable = gg_peace_settled }
				# EN/ES: CB-04 -- the branch closed before Quebracho: no resistance on the Parana, or Obligado
				#     answered by taking the mediation / la rama se cerro antes de Quebracho
				NAND = {
					has_global_variable = gg_parana_answered
					NOT = { has_global_variable = gg_parana_resistance }
				}
				NAND = {
					has_global_variable = gg_obligado_answered
					NOT = { has_global_variable = gg_parana_resistance_continues }
				}
			}
			trigger_event = { id = gg_argentina_cal.23 days = 30 }
		}
	} }
}

# EN/ES: 13/7/1846 (day/dia 3843) -- the same Hood table as gg_powers.12 / la misma mesa
gg_argentina_cal.24 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.24|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_24 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = c:CER
				has_global_variable = guerra_grande_open_war
				NOT = { has_global_variable = gg_peace_settled }
				NOT = { has_global_variable = gg_peace_offered }
				NOT = { has_global_variable = gg_hood_done }
				OR = {
					gg_plata_intervention_at = { MONTHS = 12 }
					has_global_variable = gg_powers_mediators
				}
			}
			c:ARG = { trigger_event = { id = gg_argentina.25 days = 0 popup = yes } }
		}
		# EN: C10 -- the intervention has no fixed date any more, so this relay waits for it instead of
		#     firing once on the historical day and being lost. / la intervencion ya no tiene fecha fija:
		#     este relevo la espera en vez de salir una vez el dia historico y perderse.
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_24 }
				year < 1856
				exists = s:STATE_ENTRE_RIOS
				NOT = { has_global_variable = gg_peace_settled }
				# EN/ES: CB-04 -- the Hood bases were already put to the table (gg_powers.12 on the oriental side):
				#     nothing left to wait for / las bases Hood ya se pusieron sobre la mesa
				NOT = { has_global_variable = gg_hood_done }
			}
			trigger_event = { id = gg_argentina_cal.24 days = 30 }
		}
	} }
}

# -------------------------------------------------------------------------------------------------
# EN: 15 November 1850 (day 5428) -- DESIGN DATE for the news of San Martin's death and the text
#     preserved in Balcarce's Paris copy to have crossed the Atlantic. No source used by the event
#     gives an arrival day. All delivery conditions live here, where they are read on the date that matters.
# ES: 15 de noviembre de 1850 (dia 5428) -- FECHA DE DISENO para que la noticia de la muerte de San
#     Martin y el texto conservado en la copia parisina de Balcarce hayan cruzado el Atlantico.
#     Ninguna fuente usada por el evento da un dia de llegada. Toda condicion de entrega vive aca, leida en la fecha que importa.
# SOURCE / FUENTE: death at Boulogne-sur-Mer, 17/8/1850; Balcarce copy made in Paris, 28/9/1850,
#     Sala VII, legajo 191. / muerte en Boulogne-sur-Mer, 17/8/1850; copia de Balcarce hecha en
#     Paris, 28/9/1850, Sala VII, legajo 191.
# -------------------------------------------------------------------------------------------------
gg_argentina_cal.25 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.25|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_25 }
				# EN/ES: the «la Argentina» guard; without that side, nothing changes / sin ese lado, nada cambia
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.26 days = 0 popup = yes } }
		}
	} }
}

# -------------------------------------------------------------------------------------------------
# EN: 27 November 1847 (day 4345) -- Vences is delivered only if Rosas rejected Alcaraz and the
#     actual ARG-CRR war still exists. All conditions live in this relay because a scheduled event's
#     trigger is read when it is queued, not on its delivery day. The visible event therefore has no
#     trigger of its own.
# ES: 27 de noviembre de 1847 (dia 4345) -- Vences se entrega solo si Rosas rechazo Alcaraz y la
#     guerra real ARG-CRR todavia existe. Todas las condiciones viven en este relevo porque el
#     trigger de un evento agendado se lee cuando se lo encola, no el dia de entrega. Por eso el
#     evento visible no tiene trigger propio.
# SOURCE / FUENTE: docs/guerra-grande-argentina/historia-argentina.md, cronologia 1843-1847 [D].
# -------------------------------------------------------------------------------------------------
gg_argentina_cal.26 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.26|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_26 }
				exists = s:STATE_ENTRE_RIOS
				# EN/ES: 22/9/2026 (event audit) -- Vences's army is raised in Argentine Entre Rios
				#     / el ejercito de Vences se levanta en Entre Rios argentino
				exists = s:STATE_ENTRE_RIOS.region_state:ARG
				exists = c:ARG
				exists = c:CRR
				has_global_variable = gg_alcaraz_rejected
				c:ARG = { has_war_with = c:CRR }
			}
			c:ARG = { trigger_event = { id = gg_argentina.8 days = 0 popup = yes } }
		}
	} }
}

# -------------------------------------------------------------------------------------------------
# EN: 5 January 1851 (day 5479) -- La Regeneracion. Every delivery condition lives in this relay:
#     a scheduled event reads its trigger when it is queued, not on the day it arrives. The visible
#     event therefore has no trigger of its own.
# ES: 5 de enero de 1851 (dia 5479) -- La Regeneracion. Toda condicion de entrega vive en este
#     relevo: un evento agendado lee su trigger cuando se lo encola, no el dia en que sale. Por eso
#     el evento visible no tiene trigger propio.
# SOURCE / FUENTE: docs/guerra-grande-argentina/historia-argentina.md, cronologia 1850-1852 [D].
# -------------------------------------------------------------------------------------------------
gg_argentina_cal.27 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.27|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_27 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				NOT = { exists = c:ENR }
				exists = s:STATE_ENTRE_RIOS.region_state:ARG
			}
			c:ARG = { trigger_event = { id = gg_argentina.9 days = 0 popup = yes } }
		}
	} }
}

# -------------------------------------------------------------------------------------------------
# EN: 29 May 1851 (day 5623) -- the Convention of Montevideo belongs to Entre Rios, after the
#     Pronunciamiento has created it and while the route against Rosas is still open. Every delivery
#     condition lives here because scheduled events read their trigger when they are queued.
# ES: 29 de mayo de 1851 (dia 5623) -- la Convencion de Montevideo le pertenece a Entre Rios,
#     despues de que el Pronunciamiento lo creo y mientras siga abierta la via contra Rosas. Toda
#     condicion de entrega vive aca porque los eventos agendados leen el trigger al encolarse.
# SOURCE / FUENTE: docs/guerra-grande-argentina/historia-argentina.md, «Convencion del 29/5/1851»;
# primary text / texto primario: https://www.dipublico.org/122098/
# -------------------------------------------------------------------------------------------------
gg_argentina_cal.28 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.28|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_28 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = c:ENR
				has_global_variable = gg_montevideo_convention_enabled
				NOT = { has_global_variable = gg_confederacion_negociada }
			}
			# EN/ES: 23/9 (Juan) -- nobody to sign the convention with: Urquiza fights alone, in the war his
			#     pronouncement opened; its end is read as the Pact's war / sin con quien firmar: Urquiza pelea solo
			if = {
				limit = { gg_urquiza_no_convention = yes }
				set_global_variable = gg_arg_cal_done_28
				# EN/ES: 23/9 (AUD-PACT-MARK) -- the mark and the outcome were set when the war opened (gg_argentina.3)
				#     / la marca y el resultado se pusieron al abrir la guerra
				set_global_variable = gg_war_against_buenos_aires
				c:ARG = { trigger_event = { id = gg_urquiza.2 days = 0 popup = yes } }
			}
			else = {
				c:ENR = { trigger_event = { id = gg_argentina.13 days = 0 popup = yes } }
			}
		}
		# EN: 23/9/2026 (AUD-CONV-RETRY) -- a late Pronunciamiento (gg_urquiza.1.b, 90 days on) made Entre Rios after
		#     29 May and the convention never came, nor the second one of November. While Entre Rios has not yet
		#     answered with war (gg_montevideo_convention_enabled), the relay tries again every 7 days (the answer may
		#     come any day: gg_argentina.3 waits a year); the last try lands before 21 November (gg_argentina_cal.29), so the two conventions keep their order. If the war of
		#     the Plata ended meanwhile, the try takes the branch above: Urquiza alone (gg_urquiza.2).
		# ES: un Pronunciamiento tardio creaba Entre Rios despues del 29 de mayo y la convencion no llegaba nunca, ni la
		#     segunda de noviembre. Mientras Entre Rios no haya contestado con la guerra, el relevo reintenta cada 7
		#     dias; el ultimo intento cae antes del 21 de noviembre.
		else_if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_28 }
				exists = s:STATE_ENTRE_RIOS
				c:ARG ?= this
				game_date < 1851.11.14
				NOT = { has_global_variable = gg_confederacion_negociada }
				NOT = { has_global_variable = gg_montevideo_convention_refused }
			}
			trigger_event = { id = gg_argentina_cal.28 days = 7 }
		}
	} }
}

# -------------------------------------------------------------------------------------------------
# EN: 21 November 1851 (day 5799) -- the second convention is offered only after Entre Rios signed
#     in May and Rosas activated the alliance against Buenos Aires. The visible event has no trigger.
# ES: 21 de noviembre de 1851 (dia 5799) -- la segunda convencion se ofrece solo si Entre Rios firmo
#     en mayo y Rosas activo la alianza contra Buenos Aires. El evento visible no tiene trigger.
# SOURCE / FUENTE: docs/guerra-grande-argentina/historia-argentina.md, «Convencion del 21/11/1851»;
# primary text / texto primario: https://www.dipublico.org/122100/
# -------------------------------------------------------------------------------------------------
gg_argentina_cal.29 = {
	type = country_event
	hidden = yes
	immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.29|"  hidden_effect = {
		if = {
			limit = {
				NOT = { has_global_variable = gg_arg_cal_done_29 }
				exists = s:STATE_ENTRE_RIOS
				exists = c:ARG
				exists = c:ENR
				has_global_variable = guerra_grande_treaties_signed
				has_global_variable = gg_montevideo_free_navigation
				has_global_variable = gg_war_against_buenos_aires
				NOT = { has_global_variable = gg_rosas_abandoned_oribe }
				c:ARG = { has_war_with = c:ENR }
			}
			c:ENR = { trigger_event = { id = gg_argentina.15 days = 0 popup = yes } }
		}
	} }
}

# EN: Design date 1839-09-01; conditions are evaluated on delivery, not at startup.
# ES: Fecha de diseño 1/9/1839; las condiciones se evalúan al entregar, no al iniciar.
# SOURCE / FUENTE: diagramas-y-flavour-argentino.md, LAS CINCO PRIORITARIAS.
gg_argentina_cal.30 = {
 type = country_event
 hidden = yes
 immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.30|"  hidden_effect = {
  if = {
   limit = {
    NOT = { has_global_variable = gg_arg_cal_done_30 }
    exists = s:STATE_ENTRE_RIOS
    exists = c:ARG
    c:ARG = { has_variable = gg_arg_rosas_seat }
   }
   c:ARG = { trigger_event = { id = gg_argentina.16 days = 0 popup = yes } }
  }
 } }
}

# EN: Design date 1842-06-01; conditions are evaluated on delivery, not at startup.
# ES: Fecha de diseño 1/6/1842; las condiciones se evalúan al entregar, no al iniciar.
# SOURCE / FUENTE: diagramas-y-flavour-argentino.md, LAS CINCO PRIORITARIAS.
gg_argentina_cal.31 = {
 type = country_event
 hidden = yes
 immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.31|"  hidden_effect = {
  if = {
   limit = {
    NOT = { has_global_variable = gg_arg_cal_done_31 }
    exists = s:STATE_ENTRE_RIOS
    exists = c:ARG
    c:ARG = { has_variable = gg_arg_rosas_seat }
   }
   c:ARG = { trigger_event = { id = gg_argentina.17 days = 0 popup = yes } }
  }
 } }
}

# EN: Design date 1844-07-01; conditions are evaluated on delivery, not at startup.
# ES: Fecha de diseño 1/7/1844; las condiciones se evalúan al entregar, no al iniciar.
# SOURCE / FUENTE: diagramas-y-flavour-argentino.md, LAS CINCO PRIORITARIAS.
gg_argentina_cal.32 = {
 type = country_event
 hidden = yes
 immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.32|"  hidden_effect = {
  if = {
   limit = {
    NOT = { has_global_variable = gg_arg_cal_done_32 }
    exists = s:STATE_ENTRE_RIOS
    exists = c:ARG
    c:ARG = { has_variable = gg_arg_rosas_seat }
   }
   c:ARG = { trigger_event = { id = gg_argentina.18 days = 0 popup = yes } }
  }
 } }
}

# EN: Design date 1845-03-01; conditions are evaluated on delivery, not at startup.
# ES: Fecha de diseño 1/3/1845; las condiciones se evalúan al entregar, no al iniciar.
# SOURCE / FUENTE: diagramas-y-flavour-argentino.md, LAS CINCO PRIORITARIAS.
gg_argentina_cal.33 = {
 type = country_event
 hidden = yes
 immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.33|"  hidden_effect = {
  if = {
   limit = {
    NOT = { has_global_variable = gg_arg_cal_done_33 }
    exists = s:STATE_ENTRE_RIOS
    exists = c:ARG
    c:ARG = { has_variable = gg_arg_rosas_seat }
     NOT = { has_global_variable = gg_delegation_gone_entre_rios }
     exists = s:STATE_ENTRE_RIOS.region_state:ARG
   }
   c:ARG = { trigger_event = { id = gg_argentina.19 days = 0 popup = yes } }
  }
 } }
}

# EN: Design date 1847-05-01; conditions are evaluated on delivery, not at startup.
# ES: Fecha de diseño 1/5/1847; las condiciones se evalúan al entregar, no al iniciar.
# SOURCE / FUENTE: diagramas-y-flavour-argentino.md, LAS CINCO PRIORITARIAS.
gg_argentina_cal.34 = {
 type = country_event
 hidden = yes
 immediate = { debug_log = "GG_EV|[TimeKeeper.GetCurrentDate.GetString]|gg_argentina_cal.34|"  hidden_effect = {
  if = {
   limit = {
    NOT = { has_global_variable = gg_arg_cal_done_34 }
    exists = s:STATE_ENTRE_RIOS
    exists = c:ARG
    c:ARG = { has_variable = gg_arg_rosas_seat }
     NOT = { has_global_variable = gg_delegation_gone_entre_rios }
     exists = s:STATE_ENTRE_RIOS.region_state:ARG
   }
   c:ARG = { trigger_event = { id = gg_argentina.28 days = 0 popup = yes } }
  }
 } }
}
